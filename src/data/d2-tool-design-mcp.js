// D2: Tool Design & MCP Integration — 90 questions
export const d2Questions = [
  // ============================================================
  // Task 2.1: Tool Interface Design (18 questions)
  // ============================================================
  {
    id: "d2-001",
    domain: "D2",
    taskStatement: "2.1",
    topic: "Tool Interface Design",
    difficulty: "hard",
    scenario: 1,
    importance: "Tool description is the primary selection mechanism",
    question: "A customer support resolution agent has two tools: `search_knowledge_base` (description: 'Searches internal KB articles') and `search_tickets` (description: 'Searches past support tickets'). When a customer asks about a billing dispute, Claude consistently calls `search_tickets` instead of `search_knowledge_base`, even though the KB has the authoritative billing policy. What is the most effective fix?",
    choices: { A: "Add a system prompt instruction: 'For billing questions, always use search_knowledge_base first' through the standard interface layer", B: "Rename `search_knowledge_base` to `search_billing_policies` so the name signals billing relevance per the architectural requirements", C: "Add a few-shot example showing Claude calling `search_knowledge_base` for billing questions to maintain system integrity", D: "Update the `search_knowledge_base` description to explicitly mention billing policies, refund rules, and dispute resolution procedures" },
    correct: "D",
    explanations: { A: "INCORRECT: System prompt instructions about tool selection create fragile 'ghost associations' that degrade as conversation length grows. They also don't scale when you have many routing rules. The description is the canonical place for selection hints.", B: "INCORRECT: Tool names are not the primary mechanism Claude uses for selection. While a better name might help marginally, descriptions are the authoritative signal Claude relies on to decide which tool to invoke.", C: "INCORRECT: Few-shot examples fix invocation style (how parameters are formatted, what structure the call takes), NOT selection. They do not reliably steer Claude toward choosing one tool over another for a given query type.", D: "CORRECT: Descriptions are the ONLY reliable mechanism for tool selection. By enriching the description to explicitly mention billing policies, refund rules, and dispute procedures, Claude can match the user's billing query to the correct tool. This directly addresses the root cause." }
  },
  {
    id: "d2-002",
    domain: "D2",
    taskStatement: "2.1",
    topic: "Tool Interface Design",
    difficulty: "hard",
    scenario: 3,
    importance: "Splitting overlapping tools improves selection accuracy",
    question: "A multi-agent research system has a single tool `analyze_data` with description: 'Analyzes financial data, performs statistical tests, generates visualizations, and exports reports.' Researchers report that Claude often generates a chart when they asked for a statistical test, and vice versa. What is the best architectural fix?",
    choices: { A: "Add an `action_type` enum parameter to `analyze_data` with values like 'statistics', 'visualization', 'export' to disambiguate intent", B: "Add few-shot examples demonstrating the correct parameter patterns for statistical tests vs. visualizations across all affected components", C: "Enhance the system prompt with detailed instructions on when to use each capability of `analyze_data` to maintain system integrity", D: "Split `analyze_data` into separate tools: `run_statistical_test`, `generate_visualization`, and `export_report`, each with focused descriptions" },
    correct: "D",
    explanations: { A: "INCORRECT: While adding an enum parameter helps, the core problem is that a single overloaded tool description makes selection ambiguous. Claude still has to parse one broad description and then also pick the right enum value — two opportunities for error instead of one.", B: "INCORRECT: Few-shot examples fix invocation style (how to format parameters), not tool selection logic. They won't reliably prevent Claude from choosing the wrong action within an overloaded tool.", C: "INCORRECT: System prompt instructions for tool routing create brittle associations that degrade over long conversations and don't scale. The description-per-tool approach is structurally more reliable.", D: "CORRECT: Splitting into focused tools is the recommended pattern when tools have overlapping or multiple capabilities. Each tool gets a clear, single-purpose description, which is the primary mechanism Claude uses for selection. This eliminates ambiguity at the selection stage." }
  },
  {
    id: "d2-003",
    domain: "D2",
    taskStatement: "2.1",
    topic: "Tool Interface Design",
    difficulty: "medium",
    scenario: 4,
    importance: "Few-shot examples fix invocation style, not selection",
    question: "A developer productivity tool suite includes `run_test(test_path: string, flags: string[])`. Developers complain that Claude passes flags in the wrong format — using comma-separated strings like `'--verbose,--coverage'` instead of separate array elements `['--verbose', '--coverage']`. What is the most targeted fix?",
    choices: { A: "Update the tool description to say 'flags must be passed as individual array elements, not comma-separated'", B: "Split the tool into `run_test_verbose` and `run_test_coverage` to remove the flags parameter entirely", C: "Change the parameter type from `string[]` to a single `string` to match Claude's natural formatting", D: "Add few-shot examples in the system prompt showing the correct array format for the `flags` parameter" },
    correct: "D",
    explanations: { A: "INCORRECT: Descriptions primarily drive tool selection, not parameter formatting. While adding format hints to descriptions can help marginally, few-shot examples are far more effective at demonstrating the exact call structure expected.", B: "INCORRECT: This is a massive overreaction to a formatting issue. Splitting tools eliminates useful flexibility and creates tool sprawl. The problem is invocation style, which few-shot examples directly address.", C: "INCORRECT: Changing the schema to accommodate Claude's mistakes degrades the API contract and pushes parsing complexity to the backend. The tool schema should reflect the correct interface.", D: "CORRECT: Few-shot examples are specifically designed to fix invocation style issues — how parameters are formatted, structured, and populated. This is exactly the right use case: Claude is selecting the right tool but formatting the call incorrectly." }
  },
  {
    id: "d2-004",
    domain: "D2",
    taskStatement: "2.1",
    topic: "Tool Interface Design",
    difficulty: "hard",
    scenario: 1,
    importance: "System prompt keywords create ghost tool associations",
    question: "A customer support agent's system prompt says: 'You are a helpful agent. When customers mention refunds, use the refund_processor tool to issue refunds quickly.' The agent also has a `check_eligibility` tool (description: 'Verifies whether a customer qualifies for a refund based on purchase date, product condition, and return policy'). When customers ask if they're eligible for a refund, Claude skips `check_eligibility` and goes straight to `refund_processor`. Why?",
    choices: { A: "The `check_eligibility` description is too long, causing Claude to skip reading it entirely through the standard interface layer", B: "The system prompt keyword 'refund' creates a ghost association that overrides the tool description's nuanced eligibility-checking guidance", C: "The `refund_processor` tool has a higher priority in the tool list ordering, so Claude prefers it through the standard interface layer", D: "Claude's context window is too small to process both tool descriptions simultaneously across all affected components" },
    correct: "B",
    explanations: { A: "INCORRECT: Claude does not skip reading descriptions based on length. The issue is that the system prompt instruction creates a stronger, more direct association than the description-based matching.", B: "CORRECT: System prompt keywords like 'When customers mention refunds, use refund_processor' create ghost associations — direct keyword-to-tool mappings that bypass the nuanced matching that descriptions enable. When the customer says 'refund', this association fires before Claude even evaluates `check_eligibility`'s description.", C: "INCORRECT: Tool list ordering does not establish priority for Claude's selection. Claude evaluates all available tools based on their descriptions, not their position in the list.", D: "INCORRECT: Claude's context window is large enough to handle many tool descriptions simultaneously. The issue is not capacity but the associative shortcut created by the system prompt instruction." }
  },
  {
    id: "d2-005",
    domain: "D2",
    taskStatement: "2.1",
    topic: "Tool Interface Design",
    difficulty: "medium",
    scenario: 3,
    importance: "Description specificity drives accurate selection",
    question: "A multi-agent research system for a pharmaceutical company has tools `search_pubmed` and `search_clinical_trials`. Both tools return study data. The `search_pubmed` description says 'Searches published medical literature.' The `search_clinical_trials` description says 'Searches registered clinical trials.' When a researcher asks about Phase III trial results for a new drug, Claude calls `search_pubmed` about 40% of the time. What is the best fix?",
    choices: { A: "Merge both tools into a single `search_medical` tool and add a `source` parameter, which addresses the core requirements effectively with explicit dependency management", B: "Update `search_clinical_trials` description to: 'Searches registered clinical trials including Phase I-IV studies, enrollment data, endpoints, and regulatory status'", C: "Add a system prompt rule: 'For any query mentioning trial phases, always use search_clinical_trials' to maintain system integrity", D: "Rename `search_clinical_trials` to `search_phase_trials` to include the keyword 'phase', with appropriate error handling at each step with explicit dependency management" },
    correct: "B",
    explanations: { A: "INCORRECT: Merging tools increases ambiguity. When two tools have distinct sources and use cases, keeping them separate with precise descriptions is the recommended pattern.", B: "CORRECT: Enriching the description with specific terms like 'Phase I-IV studies', 'enrollment data', 'endpoints' gives Claude much stronger matching signals. Descriptions are the primary mechanism for selection, and specificity in descriptions directly improves accuracy.", C: "INCORRECT: System prompt routing rules create fragile ghost associations. They don't generalize well to variations (e.g., 'stage III' vs 'Phase 3' vs 'pivotal trial') and degrade over long conversations.", D: "INCORRECT: Tool names are not the primary selection mechanism. Changing the name to include 'phase' provides minimal signal compared to enriching the description with comprehensive, domain-relevant terms." }
  },
  {
    id: "d2-006",
    domain: "D2",
    taskStatement: "2.1",
    topic: "Tool Interface Design",
    difficulty: "hard",
    scenario: 4,
    importance: "Tool description must differentiate overlapping capabilities",
    question: "A developer productivity platform provides Claude with `search_codebase(query: string)` (description: 'Searches the codebase for matching code') and `search_documentation(query: string)` (description: 'Searches project documentation'). When developers ask 'How does the authentication middleware work?', Claude calls both tools redundantly, increasing latency and cost. What is the root cause and fix?",
    choices: { A: "Remove `search_documentation` and rely solely on `search_codebase` since code is the source of truth, aligned with the documented best practices per the architectural requirements", B: "Set `tool_choice` to force only one tool per turn to prevent redundant calls, with appropriate error handling at each step per the architectural requirements", C: "The descriptions are too similar in structure. Rewrite them to clearly differentiate: `search_codebase` for 'finding specific code implementations, function definitions, and usage patterns' vs `search_documentation` for 'finding architectural explanations, setup guides, and design decisions'", D: "Add a pre-processing step that classifies the query and routes to exactly one tool before Claude sees it, with appropriate error handling at each step while preserving existing behavior" },
    correct: "C",
    explanations: { A: "INCORRECT: Removing documentation search eliminates a valuable resource. Many questions about 'how something works' are better answered by documentation than by raw code. The fix is better descriptions, not fewer tools.", B: "INCORRECT: Forcing one tool per turn doesn't solve the selection problem — Claude might still pick the wrong one. And it prevents legitimate cases where both sources are genuinely needed.", C: "CORRECT: The root cause is that both descriptions are vague enough that Claude can't differentiate when each is appropriate. By making descriptions specific about what each tool is best at — implementations vs. explanations — Claude can make informed selection decisions. This is the canonical fix for overlapping tool confusion.", D: "INCORRECT: Adding a pre-processing classifier adds complexity and latency, and can't leverage the conversational context Claude has. Better descriptions let Claude's built-in reasoning handle routing." }
  },
  {
    id: "d2-007",
    domain: "D2",
    taskStatement: "2.1",
    topic: "Tool Interface Design",
    difficulty: "medium",
    scenario: 1,
    importance: "Schema design affects invocation correctness",
    question: "A customer support agent has a `create_ticket` tool with parameters: `customer_id: string`, `priority: string`, `category: string`, `description: string`. Support engineers notice Claude frequently assigns arbitrary priority values like 'medium-high' or 'somewhat urgent'. What schema improvement most directly fixes this?",
    choices: { A: "Update the tool description to list all valid priority values, which maintains reliability under production load", B: "Add validation logic on the backend to reject invalid priority values and return an error for production-grade reliability", C: "Add a few-shot example showing the exact priority values to use, using the standard configuration for this scenario", D: "Change `priority` to an enum type with defined values: `['P1-Critical', 'P2-High', 'P3-Medium', 'P4-Low']`" },
    correct: "D",
    explanations: { A: "INCORRECT: Listing values in the description is less effective than using a schema enum. Descriptions are primarily for selection, and Claude may not consistently extract valid values from free-text descriptions.", B: "INCORRECT: Backend validation catches errors after the fact but creates a poor user experience — the tool call fails, Claude has to retry, adding latency and cost. Schema-level constraints prevent the error from occurring.", C: "INCORRECT: Few-shot examples help with invocation style, but they're not as reliable as schema constraints for limiting parameter values. Claude might still interpolate between examples.", D: "CORRECT: Using an enum in the JSON schema constrains Claude to only valid values at the interface level. This is the most direct and reliable fix — Claude will see the allowed values and select from them, preventing any invented values." }
  },
  {
    id: "d2-008",
    domain: "D2",
    taskStatement: "2.1",
    topic: "Tool Interface Design",
    difficulty: "hard",
    scenario: 3,
    importance: "Avoiding tool sprawl through principled design",
    question: "A multi-agent research system for a hedge fund starts with 5 well-designed tools. Over 6 months, individual analysts request specialized variants, growing to 23 tools. Analysts report that Claude now frequently selects the wrong tool or calls tools that partially overlap. What is the best remediation strategy?",
    choices: { A: "Keep all 23 tools and add few-shot examples for each one to improve selection accuracy, with appropriate error handling at each step through the standard interface layer", B: "Keep all 23 tools but add a pre-classifier agent that filters down to 5 relevant tools before passing to Claude, with proper validation at each stage using the established workflow conventions", C: "Add detailed system prompt instructions mapping each analyst's typical queries to the correct tools, using structured data for all inter-component communication", D: "Consolidate back to 8-10 tools by merging overlapping variants, using parameters to handle specialization, and writing precise descriptions that clearly differentiate each tool's purpose" },
    correct: "D",
    explanations: { A: "INCORRECT: Few-shot examples fix invocation style, not selection. Adding examples for 23 tools would consume enormous context and still wouldn't solve the fundamental selection degradation from tool sprawl.", B: "INCORRECT: While a pre-classifier can help, it adds architectural complexity and introduces its own selection errors. The root cause — too many overlapping tools — should be addressed directly through consolidation.", C: "INCORRECT: System prompt routing instructions for 23 tools would be extremely long, fragile, and create cascading ghost associations. This doesn't address the fundamental problem of tool sprawl degrading selection quality.", D: "CORRECT: Research shows that beyond roughly 15-18 tools, selection accuracy degrades significantly. Consolidating to 8-10 tools with clear, differentiated descriptions and using parameters for specialization is the recommended approach. This restores selection quality while preserving functionality." }
  },
  {
    id: "d2-009",
    domain: "D2",
    taskStatement: "2.1",
    topic: "Tool Interface Design",
    difficulty: "medium",
    scenario: 6,
    importance: "Structured output schemas improve extraction quality",
    question: "A structured data extraction pipeline uses Claude to extract financial data from invoices. The `extract_invoice` tool returns freeform JSON, and downstream systems frequently fail because field names vary ('total', 'totalAmount', 'invoice_total'). What is the best fix?",
    choices: { A: "Switch from JSON to CSV output to enforce column names, integrated into the standard development pipeline using the established workflow conventions", B: "Define a strict output schema in the tool with required fields like `total_amount`, `vendor_name`, `line_items[]` and use JSON Schema validation", C: "Add more few-shot examples showing the exact field names to use, with proper validation at each stage with proper scope boundaries", D: "Add a post-processing step to normalize field names after extraction, providing a deterministic approach to the workflow with proper scope boundaries" },
    correct: "B",
    explanations: { A: "INCORRECT: CSV is less expressive than JSON for nested data like line items. This constrains capability without addressing the root issue of undefined output structure.", B: "CORRECT: Defining a strict JSON Schema for the tool output constrains Claude to use exact field names and structures. This ensures consistency at the interface level and eliminates downstream parsing failures. Schema-driven design is the recommended approach for structured extraction.", C: "INCORRECT: Few-shot examples help but aren't sufficient — Claude may still vary field names, especially for edge cases not covered by examples. Schema constraints are more reliable.", D: "INCORRECT: Post-processing normalization is brittle and requires maintaining a mapping of all possible field name variants. It treats the symptom rather than the cause." }
  },
  {
    id: "d2-010",
    domain: "D2",
    taskStatement: "2.1",
    topic: "Tool Interface Design",
    difficulty: "hard",
    scenario: 4,
    importance: "Parameter description clarity prevents misuse",
    question: "A developer productivity tool `deploy_service(environment: string, version: string, rollback: boolean)` is used by Claude in a CI/CD context. Engineers discover that Claude sometimes sets `rollback: true` when deploying a new version, thinking it means 'enable rollback capability' rather than 'execute a rollback now'. How should this be fixed without changing the tool's behavior?",
    choices: { A: "Rename the parameter to `execute_rollback_immediately` to make intent unambiguous, with explicit handling of edge cases and boundaries to maintain system integrity", B: "Remove the `rollback` parameter and create a separate `rollback_service` tool, with clear separation between policy and implementation using the established workflow conventions", C: "Add a system prompt instruction: 'The rollback parameter means execute rollback, not enable rollback', which maintains reliability under production load per the architectural requirements", D: "Add a detailed parameter description: 'When true, IMMEDIATELY rolls back the service to the previous version instead of deploying. Does NOT enable rollback capability — it triggers an instant rollback. Default: false'" },
    correct: "D",
    explanations: { A: "INCORRECT: While a better name helps, parameter names alone aren't as effective as parameter descriptions for conveying precise semantics. Long parameter names also reduce readability.", B: "INCORRECT: Creating a separate tool is viable but overly drastic for a parameter clarity issue. This question specifically asks for a fix without changing the tool's behavior, and splitting the tool changes the interface.", C: "INCORRECT: System prompt instructions for individual parameter semantics are fragile and don't scale. If you have many tools with nuanced parameters, the system prompt becomes bloated. Parameter descriptions are the canonical location for this information.", D: "CORRECT: A detailed parameter description is the right-level fix. It clarifies the exact semantics, addresses the specific misinterpretation, and states what the parameter does NOT mean. Parameter descriptions are how you communicate precise intent for individual fields." }
  },
  {
    id: "d2-011",
    domain: "D2",
    taskStatement: "2.1",
    topic: "Tool Interface Design",
    difficulty: "hard",
    scenario: 1,
    importance: "Tools should have single clear purposes",
    question: "A customer support agent has a `manage_subscription` tool that can create, modify, cancel, pause, and resume subscriptions depending on an `action` parameter. Support staff report that Claude sometimes sends `action: 'modify'` when a customer wants to cancel, because the customer said 'change my subscription' (meaning cancel). What is the best architectural improvement?",
    choices: { A: "Add a confirmation step where Claude asks the customer to confirm the action before calling the tool, with explicit handling of edge cases and boundaries while preserving existing behavior", B: "Add few-shot examples mapping customer phrasings like 'change my subscription' to the correct action values, with explicit handling of edge cases and boundaries with proper scope boundaries", C: "Add more values to the action enum to capture nuances like 'change', 'update', 'alter', following established patterns for this architecture using the established workflow conventions", D: "Split into separate tools: `create_subscription`, `modify_subscription`, `cancel_subscription`, `pause_subscription`, `resume_subscription`, each with descriptions that include common customer phrasings" },
    correct: "D",
    explanations: { A: "INCORRECT: A confirmation step adds friction to every interaction and doesn't address the root cause. Claude is still making the wrong selection — you're just adding a manual catch.", B: "INCORRECT: Few-shot examples fix invocation style, not selection. They cannot reliably teach Claude to map ambiguous customer language to correct action parameter values.", C: "INCORRECT: Adding more enum values increases ambiguity. 'Change' could still mean modify or cancel depending on context. More options make correct selection harder, not easier.", D: "CORRECT: Splitting into separate tools with focused descriptions is the recommended pattern. Each tool's description can include the specific customer phrasings that should trigger it (e.g., cancel_subscription: 'Cancels a subscription. Use when customer wants to end, stop, discontinue, or change away from their plan'). Descriptions drive selection, and focused tools have clearer descriptions." }
  },
  {
    id: "d2-012",
    domain: "D2",
    taskStatement: "2.1",
    topic: "Tool Interface Design",
    difficulty: "medium",
    scenario: 3,
    importance: "Tool descriptions should include negative guidance",
    question: "A research system has `query_internal_database` and `query_external_api`. For compliance reasons, certain queries containing PII must NEVER go to the external API. Claude occasionally sends PII-containing queries externally. What is the most effective tool-level mitigation?",
    choices: { A: "Add to `query_external_api` description: 'NEVER send queries containing names, SSNs, emails, or other PII. For PII-containing queries, use query_internal_database instead.'", B: "Add a backend filter that strips PII from queries before they reach the external API, with appropriate error handling at each step according to the configured policy", C: "Add a system prompt instruction about PII handling across all tools, which addresses the core requirements effectively according to the configured policy", D: "Remove `query_external_api` from the tool set and proxy all external queries through the internal database with explicit dependency management" },
    correct: "A",
    explanations: { A: "CORRECT: Adding negative guidance directly in the tool description is highly effective. Descriptions drive selection, and explicit 'do NOT use this tool when...' guidance helps Claude route correctly. This is the recommended approach for compliance-sensitive routing.", B: "INCORRECT: Stripping PII at the backend is a defense-in-depth measure but doesn't prevent the tool from being called — the query still reaches the external service, just with PII removed, which may corrupt the query intent.", C: "INCORRECT: System prompt instructions about PII create general awareness but don't provide the tool-specific routing guidance that descriptions offer. Tool-level guidance is more reliable for tool-level decisions.", D: "INCORRECT: Removing the external API tool eliminates legitimate, non-PII queries to external sources. This is overly restrictive and reduces functionality." }
  },
  {
    id: "d2-013",
    domain: "D2",
    taskStatement: "2.1",
    topic: "Tool Interface Design",
    difficulty: "hard",
    scenario: 4,
    importance: "Progressive disclosure in tool parameters",
    question: "A developer productivity tool `configure_build(target: string, optimization: string, debug: boolean, cache: boolean, parallel: number, timeout: number, env_vars: object, artifacts: string[], hooks: object)` has 9 parameters. Claude frequently omits required parameters or provides defaults that don't match the project. What redesign principle best addresses this?",
    choices: { A: "Provide few-shot examples covering all common parameter combinations, following established patterns for this architecture through the standard interface layer", B: "Make all parameters required so Claude must explicitly set each one, ensuring consistent behavior across all sessions through the standard interface layer", C: "Split into 9 separate tools, one for each configuration aspect, aligned with the documented best practices across all affected components", D: "Reduce to 3 essential required parameters (`target`, `optimization`, `debug`) with sensible server-side defaults for the rest, and group optional parameters into a single `advanced_options` object" },
    correct: "D",
    explanations: { A: "INCORRECT: Few-shot examples for 9-parameter combinations would require many examples to cover common cases and would consume significant context. This treats the symptom rather than the design issue.", B: "INCORRECT: Making all 9 parameters required increases cognitive load and error rate. Claude must guess values for parameters the user didn't mention, leading to incorrect configurations.", C: "INCORRECT: Splitting into 9 tools creates massive tool sprawl and breaks the logical cohesion of build configuration. Individual configuration aspects are not meaningful operations on their own.", D: "CORRECT: Progressive disclosure — keeping essential parameters prominent and grouping optional ones — reduces cognitive load for Claude while preserving flexibility. Server-side defaults handle the common case, and the optional `advanced_options` object is available when needed." }
  },
  {
    id: "d2-014",
    domain: "D2",
    taskStatement: "2.1",
    topic: "Tool Interface Design",
    difficulty: "medium",
    scenario: 6,
    importance: "Return value descriptions guide Claude's response formatting",
    question: "A structured data extraction tool returns a JSON object with fields `confidence_score`, `extracted_entities`, and `validation_warnings`. Claude consistently ignores the `validation_warnings` array and presents results as if extraction was successful. What is the most targeted fix?",
    choices: { A: "Change the return type to put warnings first in the JSON object ordering, with appropriate error handling at each step with proper scope boundaries", B: "Move warnings into the `extracted_entities` array so they can't be missed, following the recommended implementation pattern according to the configured policy", C: "Add a separate `check_warnings` tool that Claude must call after extraction, following the recommended implementation pattern to maintain system integrity", D: "Add to the tool description: 'IMPORTANT: Always check and report validation_warnings to the user before presenting extracted_entities. Warnings indicate potential extraction errors.'" },
    correct: "D",
    explanations: { A: "INCORRECT: JSON object field ordering does not affect how Claude processes the response. Claude reads the full response regardless of field order.", B: "INCORRECT: Mixing warnings into entity data pollutes the data structure, complicates downstream processing, and conflates two different types of information.", C: "INCORRECT: Adding a separate tool for warning checks adds unnecessary roundtrips and complexity. The warnings are already in the response — Claude just needs guidance to surface them.", D: "CORRECT: Adding explicit guidance in the tool description about how to handle return values — especially which fields require attention — is the right approach. Descriptions guide both selection and post-call behavior." }
  },
  {
    id: "d2-015",
    domain: "D2",
    taskStatement: "2.1",
    topic: "Tool Interface Design",
    difficulty: "hard",
    scenario: 1,
    importance: "Tool versioning and backward compatibility",
    question: "A customer support platform is upgrading its `lookup_customer` tool to return additional fields (loyalty_tier, lifetime_value). The new schema is backward-compatible but adds optional fields. 200 deployed agent instances use this tool. What is the safest deployment approach?",
    choices: { A: "Update the existing tool's schema and description to mention the new fields, deploy to all instances, and let Claude discover the new fields organically", B: "Deploy the new schema to all instances simultaneously since it's backward-compatible, ensuring consistent behavior across all sessions", C: "Create a new tool `lookup_customer_v2` and gradually migrate agents while keeping v1 available across all affected components", D: "Add the new fields to the backend response without updating the schema, so Claude receives them but isn't told about them while preserving existing behavior" },
    correct: "A",
    explanations: { A: "CORRECT: Since the schema change is backward-compatible (adding optional fields), the right approach is to update the schema and description together so Claude knows about the new fields. The description should mention how to use loyalty_tier and lifetime_value. This is a safe single deployment since nothing breaks for existing queries.", B: "INCORRECT: While technically backward-compatible, deploying without updating descriptions means Claude won't know about the new fields and can't leverage them. Description updates are needed for Claude to use new capabilities.", C: "INCORRECT: Creating a v2 tool doubles the tool count and creates confusion about which version to use. Backward-compatible additions don't require a new tool — they can be added to the existing schema.", D: "INCORRECT: Adding fields without updating the schema means Claude has no guidance on what the fields mean or when to use them. While Claude might notice extra fields in responses, without schema and description support, usage will be inconsistent." }
  },
  {
    id: "d2-016",
    domain: "D2",
    taskStatement: "2.1",
    topic: "Tool Interface Design",
    difficulty: "medium",
    scenario: 3,
    importance: "Idempotent tool design for reliability",
    question: "A research system's `submit_analysis_job(dataset_id: string, analysis_type: string)` tool sometimes gets called twice when Claude retries due to a timeout. This creates duplicate analysis jobs, wasting compute. What tool design principle should be applied?",
    choices: { A: "Make the tool description say 'Do not retry this tool if it times out' per the architectural requirements", B: "Add a `request_id` parameter that Claude generates as a UUID, so the backend can deduplicate", C: "Add a `check_job_status` tool and instruct Claude to check before resubmitting according to the configured policy", D: "Add rate limiting to prevent Claude from calling the tool more than once per minute" },
    correct: "B",
    explanations: { A: "INCORRECT: Description instructions not to retry are unreliable. Claude may retry if it believes the tool didn't execute, and the description guidance can be overridden by the model's retry heuristics.", B: "CORRECT: Adding an idempotency key (`request_id`) is a standard pattern for making tools safe to retry. If Claude sends the same `request_id` twice, the backend recognizes the duplicate and returns the existing job instead of creating a new one. This makes the tool inherently safe for retries.", C: "INCORRECT: While `check_job_status` is useful, requiring Claude to check before every submission adds latency and doesn't guarantee the check-then-submit sequence is atomic. An idempotency key is a more robust solution.", D: "INCORRECT: Rate limiting prevents rapid calls but doesn't solve the duplicate problem — Claude could retry after the rate limit window. It also blocks legitimate rapid successive calls for different analyses." }
  },
  {
    id: "d2-017",
    domain: "D2",
    taskStatement: "2.1",
    topic: "Tool Interface Design",
    difficulty: "hard",
    scenario: 5,
    importance: "Tool design for CI/CD safety",
    question: "Claude Code is used in a CI/CD pipeline with a `deploy_to_production(service: string, version: string)` tool. The team wants to ensure Claude cannot accidentally deploy untested versions. What tool design approach best enforces this safety constraint?",
    choices: { A: "Add a pre-deploy confirmation tool that Claude must call first, with proper validation at each stage according to the configured policy", B: "Redesign the tool to `deploy_to_production(service: string, version: string, test_run_id: string)` where `test_run_id` is a required parameter that the backend validates against a passing test suite", C: "Add a description warning: 'Only deploy versions that have passed all tests. Check test status before deploying.', integrated into the standard development pipeline using the established workflow conventions", D: "Use `tool_choice: none` to disable the deploy tool by default and only enable it after tests pass, with built-in recovery for expected failure modes using the established workflow conventions" },
    correct: "B",
    explanations: { A: "INCORRECT: A confirmation tool adds a step but doesn't enforce the constraint. Claude could skip the confirmation or the confirmation tool could lack the authority to actually verify test status.", B: "CORRECT: Making `test_run_id` a required parameter that the backend validates creates a structural enforcement. Claude cannot deploy without providing a valid, passing test run ID. This is design-level safety — the tool physically cannot be misused because the backend rejects calls without proof of passing tests.", C: "INCORRECT: Description warnings are advisory and cannot enforce constraints. Claude might skip the check or misinterpret test results. Safety-critical constraints need structural enforcement.", D: "INCORRECT: `tool_choice: none` disables ALL tool use, not just one specific tool. There's no mechanism to dynamically enable/disable individual tools based on external state through tool_choice alone." }
  },
  {
    id: "d2-018",
    domain: "D2",
    taskStatement: "2.1",
    topic: "Tool Interface Design",
    difficulty: "medium",
    scenario: 6,
    importance: "Handling ambiguous tool boundaries",
    question: "A document processing system has `extract_table(page: number)` and `extract_text(page: number)`. When users upload PDFs with tables embedded in paragraphs, Claude can't decide which tool to use and often calls both sequentially, doubling processing time. What is the best design improvement?",
    choices: { A: "Add a `detect_content_type(page: number)` tool that Claude calls first to determine the page layout per the architectural requirements", B: "Add a system prompt rule: 'For mixed content pages, always use extract_text first, then extract_table only if tables are detected' to maintain system integrity", C: "Update both descriptions to say 'Use for pages that contain ONLY tables/text respectively', following established patterns for this architecture", D: "Merge into a single `extract_content(page: number, content_type: 'table' | 'text' | 'all')` tool with a clear description of when to use each content type" },
    correct: "D",
    explanations: { A: "INCORRECT: Adding a detection tool means three calls instead of two, increasing latency further. The content type detection logic belongs in the extraction tool itself.", B: "INCORRECT: System prompt sequential rules are fragile and add latency. The two-call sequence is the exact problem being solved.", C: "INCORRECT: Restricting descriptions to 'ONLY tables' or 'ONLY text' means mixed-content pages genuinely need both calls, which doesn't solve the performance issue.", D: "CORRECT: When two tools frequently need to be called together or have overlapping scope, merging them with a discriminating parameter is the right pattern. The `content_type: 'all'` option handles the common mixed-content case in a single call, reducing latency and cost." }
  },

  // ============================================================
  // Task 2.2: Structured Error Responses (18 questions)
  // ============================================================
  {
    id: "d2-019",
    domain: "D2",
    taskStatement: "2.2",
    topic: "Structured Error Responses",
    difficulty: "hard",
    scenario: 1,
    importance: "isError is protocol-level, not just an error string",
    question: "A customer support agent calls a `refund_customer` MCP tool. The tool returns: `{ content: [{ type: 'text', text: 'Error: Customer account is frozen due to fraud investigation' }] }`. Claude presents this error text directly to the customer as if it were a normal response. What is missing from the tool's error handling?",
    choices: { A: "The response should include `isError: true` at the MCP protocol level so Claude knows to handle it as an error rather than a normal result", B: "The tool should throw an exception instead of returning an error object, aligned with the documented best practices across all affected components", C: "The error message should be wrapped in an HTML error tag for Claude to recognize it for production-grade reliability", D: "The error should be returned as an HTTP 500 status code instead of in the response body through the standard interface layer" },
    correct: "A",
    explanations: { A: "CORRECT: In the MCP protocol, `isError: true` is a protocol-level flag that signals to Claude that the response represents an error condition, not a normal result. Without this flag, Claude treats the error text as regular content and may surface internal error details to the user.", B: "INCORRECT: Throwing exceptions in an MCP tool handler would cause the MCP server to crash or return a transport-level error, not a structured error that Claude can reason about and handle gracefully.", C: "INCORRECT: HTML tags in tool responses don't signal error status to Claude. Claude processes the text content regardless of HTML formatting.", D: "INCORRECT: MCP tool responses don't use HTTP status codes for error signaling within the protocol. The `isError` flag is the MCP-native mechanism for error indication." }
  },
  {
    id: "d2-020",
    domain: "D2",
    taskStatement: "2.2",
    topic: "Structured Error Responses",
    difficulty: "hard",
    scenario: 3,
    importance: "isRetryable prevents wasted retries on permanent failures",
    question: "A multi-agent research system's `access_proprietary_database` tool sometimes fails with 'Access denied: subscription expired'. Claude automatically retries the call 3 times, wasting API credits. What structured error field should be added to prevent this?",
    choices: { A: "Add `isRetryable: false` to the error response to signal that retrying will not resolve this error", B: "Add `maxRetries: 0` to the tool configuration to disable retries globally across all affected components", C: "Add 'Do not retry this error' to the error message text, following the recommended implementation pattern", D: "Add `severity: 'fatal'` to the error response to signal Claude should stop with proper scope boundaries" },
    correct: "A",
    explanations: { A: "CORRECT: `isRetryable: false` is the structured mechanism to tell Claude (or the orchestrator) that this error is permanent and retrying will not help. An expired subscription is a policy/administrative issue that cannot be resolved by calling the tool again. This prevents wasted retries.", B: "INCORRECT: Disabling retries globally would prevent retries even for transient errors (network timeouts, rate limits) where retrying is the correct behavior. The retryability signal should be per-error, not per-tool.", C: "INCORRECT: Natural language instructions in error messages are less reliable than structured fields. Claude may or may not honor free-text retry instructions, especially under agentic orchestration where retry logic may be automated.", D: "INCORRECT: `severity` is not a standard MCP error response field. Custom fields may not be recognized by the framework." }
  },
  {
    id: "d2-021",
    domain: "D2",
    taskStatement: "2.2",
    topic: "Structured Error Responses",
    difficulty: "medium",
    scenario: 1,
    importance: "Access failure is not the same as empty result",
    question: "A customer support agent calls `search_orders(customer_id: '12345')` and the tool returns: `{ content: [{ type: 'text', text: '[]' }] }`. Claude tells the customer 'You have no orders on file.' However, the empty array was actually caused by a database connection timeout — the customer has 15 orders. What error handling principle was violated?",
    choices: { A: "The tool should have returned the error with `isError: true` and a message indicating the database was unreachable, rather than returning an empty result", B: "Claude should have been instructed to always double-check empty results, leveraging the native capabilities of the platform per the architectural requirements", C: "The system prompt should tell Claude to never trust empty results, with appropriate error handling at each step for production-grade reliability", D: "The tool should have returned `null` instead of an empty array to signal failure, with explicit handling of edge cases and boundaries" },
    correct: "A",
    explanations: { A: "CORRECT: An access failure (database timeout) must NEVER be returned as a valid empty result. The tool must distinguish between 'successfully searched and found nothing' vs 'failed to search at all'. Returning `isError: true` with a descriptive message lets Claude inform the customer accurately and potentially retry.", B: "INCORRECT: Instructing Claude to double-check all empty results wastes API calls on genuinely empty results and doesn't address the root cause — the tool is misreporting failures as empty results.", C: "INCORRECT: Making Claude distrust all empty results leads to unnecessary follow-up calls and an inconsistent user experience. The tool should accurately report its state.", D: "INCORRECT: `null` vs `[]` is a subtle data distinction that Claude may not interpret as an error signal. The `isError` flag is the unambiguous protocol-level mechanism for error reporting." }
  },
  {
    id: "d2-022",
    domain: "D2",
    taskStatement: "2.2",
    topic: "Structured Error Responses",
    difficulty: "hard",
    scenario: 4,
    importance: "Local recovery for transient errors; propagate unresolvable",
    question: "A developer productivity agent calls a `fetch_deployment_logs(service: string, timeframe: string)` tool that fails with a 429 rate limit error. The agent is in the middle of a complex debugging session with the developer. What is the correct error handling strategy?",
    choices: { A: "Cache the error and continue the debugging session without logs, hoping the developer doesn't notice to maintain system integrity", B: "Switch to a different tool that doesn't have rate limits, with proper validation at each stage with proper scope boundaries", C: "Handle the transient error locally — implement exponential backoff retry within the tool, and only propagate to the user if retries are exhausted", D: "Propagate the error to the developer immediately: 'The log service is rate limited. Please try again later.' through the standard interface layer" },
    correct: "C",
    explanations: { A: "INCORRECT: Silently swallowing errors and proceeding without data leads to incorrect conclusions. The developer needs accurate information for debugging.", B: "INCORRECT: Switching tools because of a transient error is an overreaction. The rate limit will resolve, and an alternative tool may not provide the same data quality.", C: "CORRECT: Transient errors (rate limits, network blips, temporary unavailability) should be handled locally with retry logic — exponential backoff is the standard pattern for 429s. Only if retries are exhausted should the error be propagated to the user. This maintains a smooth experience during the debugging session.", D: "INCORRECT: A 429 rate limit is a transient error that typically resolves within seconds. Immediately propagating it breaks the developer's workflow unnecessarily. Local recovery should be attempted first." }
  },
  {
    id: "d2-023",
    domain: "D2",
    taskStatement: "2.2",
    topic: "Structured Error Responses",
    difficulty: "medium",
    scenario: 1,
    importance: "Customer-friendly error messages for business errors",
    question: "A customer support agent's `process_return` tool returns: `{ isError: true, content: [{ type: 'text', text: 'SQLException: RETURN_POLICY_VIOLATION - item.purchase_date < DATEADD(day, -30, GETDATE()) in policy_check.sp line 47' }] }`. Claude relays this technical error to the customer verbatim. What should the tool return instead?",
    choices: { A: "A JSON error code like `{ errorCode: 'RETURN_POLICY_30DAY' }` that Claude can look up in its training data with explicit dependency management", B: "Just `isError: true` with no message, letting Claude generate an appropriate response, aligned with the documented best practices across all affected components", C: "The same SQL error but with `isError: false` so Claude treats it as informational, scoped to the relevant context for this operation for production-grade reliability", D: "A customer-friendly message: 'This item is not eligible for return because it was purchased more than 30 days ago. Please contact support for exceptions.' with `isError: true`" },
    correct: "D",
    explanations: { A: "INCORRECT: Claude does not have a lookup table of application-specific error codes. While error codes are useful for backend systems, the tool response must include human-readable context for Claude to work with.", B: "INCORRECT: Returning `isError: true` without a message forces Claude to guess what went wrong. Claude may fabricate an incorrect explanation or provide generic unhelpful text.", C: "INCORRECT: Setting `isError: false` would make Claude treat the SQL error as a normal result and present it even more directly to the customer. This makes the problem worse.", D: "CORRECT: Business errors that may be surfaced to customers must include customer-friendly messages. The tool should translate internal technical errors into clear, actionable language. Keeping `isError: true` ensures Claude handles it as an error condition while having a presentable message." }
  },
  {
    id: "d2-024",
    domain: "D2",
    taskStatement: "2.2",
    topic: "Structured Error Responses",
    difficulty: "hard",
    scenario: 3,
    importance: "Partial results with degradation warnings",
    question: "A research system's `aggregate_market_data` tool queries 5 data sources. During a query, 2 sources time out but 3 return valid data. The tool currently returns either all data or an error. A financial analyst needs the available data urgently. What is the best error response design?",
    choices: { A: "Return partial results with a structured response: `{ isError: false, content: [...results...], _meta: { sources_queried: 5, sources_succeeded: 3, failed_sources: ['source_4', 'source_5'], degradation_warning: 'Results are partial. 2 of 5 sources timed out.' } }`", B: "Return `isError: true` since the full dataset is incomplete, forcing a retry, with built-in recovery for expected failure modes with proper scope boundaries", C: "Return the 3 successful results with `isError: false` and silently drop the 2 failed sources, supporting both automated and manual processes across all affected components", D: "Return the 3 results but set `isError: true` to flag the degradation, with proper validation at each stage while preserving existing behavior" },
    correct: "A",
    explanations: { A: "CORRECT: Returning partial results with a structured degradation warning is the best pattern. `isError: false` because valid data was returned, but the metadata clearly communicates which sources failed and that results are partial. Claude can present the data while warning the analyst about the limitation.", B: "INCORRECT: Returning a full error discards 3 valid results that the analyst urgently needs. A retry might also fail on the same sources, causing a complete deadlock.", C: "INCORRECT: Silently dropping failed sources is dangerous in financial analysis. The analyst might make decisions not knowing their data is incomplete. Transparency about data completeness is essential.", D: "INCORRECT: Setting `isError: true` signals to Claude that the entire response is an error, which may cause it to discard the valid results or present them with inappropriate error framing." }
  },
  {
    id: "d2-025",
    domain: "D2",
    taskStatement: "2.2",
    topic: "Structured Error Responses",
    difficulty: "medium",
    scenario: 4,
    importance: "Error categorization drives correct recovery behavior",
    question: "A developer productivity agent encounters three different errors: (1) 'Rate limit exceeded', (2) 'Invalid API key', (3) 'File not found: /src/utils.ts'. Which categorization correctly maps each error to the right recovery strategy?",
    choices: { A: "(1) Transient → retry with backoff; (2) Configuration → propagate to user for key update; (3) Input → adjust parameters and retry with correct path", B: "All three are transient errors — retry all with exponential backoff, supporting both automated and manual processes with explicit dependency management", C: "(1) Transient → retry; (2) Transient → retry with different key; (3) Fatal → stop all operations through the standard interface layer", D: "(1) Fatal → stop; (2) Configuration → propagate; (3) Transient → retry with same path, aligned with the documented best practices" },
    correct: "A",
    explanations: { A: "CORRECT: Rate limits are transient (resolve with time). Invalid API keys are configuration errors that require human intervention (updating the key). File not found is an input error — Claude should adjust the file path based on codebase knowledge and retry. Each category has a distinct recovery strategy.", B: "INCORRECT: An invalid API key will never resolve through retrying — it's a configuration error. A missing file is an input error, not a transient one. Retrying both wastes resources.", C: "INCORRECT: An invalid API key is not transient — trying a 'different key' assumes Claude has alternative credentials, which it shouldn't. File not found is not fatal — the file may exist at a different path.", D: "INCORRECT: Rate limits are not fatal — they resolve with time and backoff. A missing file is not transient — the same path will fail again. This categorization inverts the correct recovery strategies." }
  },
  {
    id: "d2-026",
    domain: "D2",
    taskStatement: "2.2",
    topic: "Structured Error Responses",
    difficulty: "hard",
    scenario: 1,
    importance: "Error responses should include actionable context",
    question: "A support agent's `transfer_to_specialist` tool fails with: `{ isError: true, content: [{ type: 'text', text: 'Transfer failed' }] }`. Claude tells the customer 'I was unable to transfer you' and stops. The customer is frustrated. What error response improvement would enable Claude to handle this gracefully?",
    choices: { A: "Add `isRetryable: true` so Claude keeps retrying until it succeeds, maintaining compatibility with existing workflows through the standard interface layer", B: "Remove `isError: true` so Claude doesn't give up, with explicit handling of edge cases and boundaries through the standard interface layer", C: "Add a longer error message: 'Transfer failed. The specialist queue is currently full. Estimated wait time: 15 minutes. Alternative: The customer can leave a callback number.'", D: "Include a fallback tool suggestion: `{ suggestedAction: 'use escalate_ticket instead' }`, which addresses the core requirements effectively according to the configured policy" },
    correct: "C",
    explanations: { A: "INCORRECT: Blindly retrying when the queue is full will fail repeatedly. While `isRetryable` is useful for transient errors, the tool should also provide context about when retry might succeed and what alternatives exist.", B: "INCORRECT: Removing `isError: true` makes Claude treat the 'Transfer failed' text as a normal result, which is even worse — it might present it as if the transfer succeeded.", C: "CORRECT: Error responses should include actionable context — the reason for failure, the current state, and alternative paths forward. This enables Claude to present a helpful response: 'The specialist team is currently at capacity. I can either try again in about 15 minutes, or take your callback number so a specialist can reach you directly.'", D: "INCORRECT: `suggestedAction` is not a standard MCP error response field. While the idea of suggesting alternatives is good, this should be communicated through the error message text, not a non-standard field." }
  },
  {
    id: "d2-027",
    domain: "D2",
    taskStatement: "2.2",
    topic: "Structured Error Responses",
    difficulty: "hard",
    scenario: 3,
    importance: "Validation errors should guide correction",
    question: "A research system's `submit_query` tool receives a complex query object. When validation fails, it returns: `{ isError: true, content: [{ type: 'text', text: 'Invalid query' }] }`. Claude tries different random query formats, burning through 5 retries before giving up. What error response pattern fixes this?",
    choices: { A: "Add more query format examples to the tool description, integrated into the standard development pipeline for production-grade reliability", B: "Set `isRetryable: false` to prevent all retries on validation errors, with appropriate error handling at each step for production-grade reliability", C: "Add a `validate_query` tool that Claude calls first to check before submitting, scoped to the relevant context for this operation with proper scope boundaries", D: "Return specific validation errors: `{ isError: true, isRetryable: true, content: [{ type: 'text', text: 'Validation failed: date_range.start must be in ISO-8601 format (got \"last week\"). Expected: \"2025-01-01\". filters.category must be one of [\"equity\", \"fixed_income\", \"derivatives\"].' }] }`" },
    correct: "D",
    explanations: { A: "INCORRECT: More examples in the description help with initial formatting but don't help when a specific validation fails. Runtime validation errors need runtime feedback.", B: "INCORRECT: Setting `isRetryable: false` for validation errors is wrong — these errors ARE fixable by adjusting the query. Preventing retries forces the user to manually reformulate when Claude could self-correct.", C: "INCORRECT: A separate validation tool adds an extra round-trip to every query. The submit tool should return detailed validation errors itself — validation is an inherent part of processing.", D: "CORRECT: Specific validation errors with the exact problem, the received value, and the expected format allow Claude to correct its query precisely on the first retry. This is dramatically more efficient than vague 'Invalid query' messages that force trial-and-error." }
  },
  {
    id: "d2-028",
    domain: "D2",
    taskStatement: "2.2",
    topic: "Structured Error Responses",
    difficulty: "medium",
    scenario: 6,
    importance: "Error handling in extraction pipelines",
    question: "A document extraction tool encounters a scanned PDF with poor OCR quality. It extracts some fields but with low confidence. What is the most appropriate response pattern?",
    choices: { A: "Return `isError: true` with 'OCR quality too low for reliable extraction', with built-in recovery for expected failure modes according to the configured policy", B: "Return only the high-confidence fields and silently drop low-confidence ones, maintaining compatibility with existing workflows with explicit dependency management", C: "Return all fields without confidence scores to avoid confusing the user, using the standard configuration for this scenario with explicit dependency management", D: "Return the extracted fields with per-field confidence scores and a warning: `{ confidence: { vendor_name: 0.92, total: 0.45, date: 0.31 }, warning: 'Low OCR quality. Fields with confidence < 0.7 may be inaccurate and should be verified manually.' }`" },
    correct: "D",
    explanations: { A: "INCORRECT: Returning a blanket error discards the fields that were extracted with high confidence (vendor_name at 0.92). Partial results with quality indicators are more useful.", B: "INCORRECT: Silently dropping fields means the user doesn't know what was missed. They might assume the document had no date when actually the date was extracted but with low confidence.", C: "INCORRECT: Omitting confidence scores in a low-quality extraction scenario is dangerous. Users may trust inaccurate fields, leading to data integrity issues downstream.", D: "CORRECT: Per-field confidence scores let Claude (and the user) make informed decisions about which fields to trust and which to verify. The warning sets clear expectations. This pattern is essential in extraction pipelines where quality varies." }
  },
  {
    id: "d2-029",
    domain: "D2",
    taskStatement: "2.2",
    topic: "Structured Error Responses",
    difficulty: "hard",
    scenario: 4,
    importance: "Authorization errors need distinct handling from other errors",
    question: "A developer productivity agent calls `delete_branch(branch: 'main')` and the tool returns `{ isError: true, content: [{ type: 'text', text: 'Permission denied: protected branch' }] }`. Claude retries with different parameters (`force: true`). This is dangerous. What error response improvement prevents this?",
    choices: { A: "Return the error without `isError: true` so Claude treats it as information, using the standard configuration for this scenario through the standard interface layer", B: "Add `isRetryable: false` and categorize as a policy error: 'Policy violation: Cannot delete protected branches. This is an organizational policy, not a technical limitation. No parameter changes will bypass this restriction.'", C: "Add `isRetryable: false` without additional context, following established patterns for this architecture through the standard interface layer", D: "Add a system prompt rule: 'Never retry permission denied errors', using the standard configuration for this scenario for production-grade reliability" },
    correct: "B",
    explanations: { A: "INCORRECT: Omitting `isError: true` makes Claude treat 'Permission denied: protected branch' as a normal result, which could lead to even more confused behavior.", B: "CORRECT: The error response must combine `isRetryable: false` with explicit context explaining this is a POLICY restriction, not a technical one. Without this context, Claude might infer that different parameters could work. The message 'no parameter changes will bypass this restriction' directly prevents the dangerous retry-with-force behavior.", C: "INCORRECT: `isRetryable: false` alone prevents retrying with the same parameters, but Claude might interpret 'permission denied' as fixable by changing parameters (adding force, changing auth). The message must explicitly state no parameters will help.", D: "INCORRECT: System prompt rules are fragile and may not cover all variations of permission errors. The error response itself must carry the retry guidance." }
  },
  {
    id: "d2-030",
    domain: "D2",
    taskStatement: "2.2",
    topic: "Structured Error Responses",
    difficulty: "medium",
    scenario: 1,
    importance: "Timeout errors need context for intelligent retry",
    question: "A customer support agent's `search_orders` tool times out after 30 seconds. The agent has `isRetryable: true` in the error. Claude retries immediately and gets another timeout. What additional error context would help?",
    choices: { A: "A `retryAfterMs: 5000` field and the original query parameters so Claude can retry identically after a delay to maintain system integrity", B: "A message suggesting the search criteria are too broad: 'Search timed out. Try narrowing the date range or adding more specific filters to reduce results.'", C: "An `errorCode: 'TIMEOUT'` field for programmatic handling, with built-in recovery for expected failure modes to maintain system integrity", D: "A stack trace so Claude can debug the backend, leveraging the native capabilities of the platform using the established workflow conventions" },
    correct: "B",
    explanations: { A: "INCORRECT: Retrying identically after a delay will likely produce the same timeout — the query itself is the problem, not a transient server issue. A delay helps with rate limits, not with computation timeouts.", B: "CORRECT: When a search times out, it often means the query is too broad. Providing actionable guidance to narrow the search lets Claude reformulate (e.g., add a date range filter) rather than retrying the same slow query. This is much more likely to succeed on the next attempt.", C: "INCORRECT: An error code alone doesn't help Claude take corrective action. Claude needs human-readable guidance on how to adjust its approach.", D: "INCORRECT: Stack traces expose internal implementation details and are not actionable by Claude. They're a security concern and provide no useful guidance for query optimization." }
  },
  {
    id: "d2-031",
    domain: "D2",
    taskStatement: "2.2",
    topic: "Structured Error Responses",
    difficulty: "hard",
    scenario: 3,
    importance: "Chained tool errors need upstream context",
    question: "A research system has a pipeline: `fetch_data` → `transform_data` → `generate_report`. The `transform_data` step fails because `fetch_data` returned malformed data (but reported success). Claude has no visibility into why transformation failed. What error design principle should be applied?",
    choices: { A: "Make `fetch_data` always return data in a validated schema so downstream tools never fail, aligned with the documented best practices across all affected components", B: "Add logging to each tool so engineers can debug after the fact, using structured data for all inter-component communication per the architectural requirements", C: "Add a global error handler that catches all pipeline errors, using structured data for all inter-component communication with proper scope boundaries", D: "Each tool should validate its inputs and return errors that reference the upstream step: 'Transform failed: Input data from fetch_data is missing required field \"timestamp\" in records 15-23. Suggest re-fetching with format=\"structured\".'" },
    correct: "D",
    explanations: { A: "INCORRECT: While input validation at `fetch_data` is good practice, it cannot guarantee all downstream requirements are met. Each tool must validate its own inputs as a defense-in-depth measure.", B: "INCORRECT: Backend logging helps engineers but doesn't help Claude or the user in real-time. The error needs to be actionable within the current conversation.", C: "INCORRECT: A global error handler catches errors but loses the specific context of where and why the failure occurred. Claude needs granular, per-step error information.", D: "CORRECT: In chained tool pipelines, downstream tools should validate their inputs and produce errors that reference the upstream source. This gives Claude enough context to either re-invoke the upstream tool with corrections or inform the user about the specific failure point in the pipeline." }
  },
  {
    id: "d2-032",
    domain: "D2",
    taskStatement: "2.2",
    topic: "Structured Error Responses",
    difficulty: "medium",
    scenario: 6,
    importance: "Rate limit errors need specific retry guidance",
    question: "A data extraction pipeline hits an external API rate limit. The error returns `{ isError: true, isRetryable: true, content: [{ type: 'text', text: 'Rate limited' }] }`. Claude retries immediately and gets rate limited again. What field should the error response include?",
    choices: { A: "A `priority` field to queue the retry: `{ priority: 'high' }` with explicit dependency management", B: "A `retryAfter` field indicating how many seconds to wait: `{ retryAfter: 60 }`", C: "A `maxRetries` field: `{ maxRetries: 3 }`, applied systematically across the entire system", D: "A `fallbackTool` field: `{ fallbackTool: 'extract_from_cache' }` with proper scope boundaries" },
    correct: "B",
    explanations: { A: "INCORRECT: `priority` is not a standard error response field and doesn't address the core issue of retry timing.", B: "CORRECT: Rate limit errors should include a `retryAfter` delay so Claude (or the orchestrator) knows exactly how long to wait before retrying. This prevents the rapid-retry loop that wastes API calls and extends the rate limit window.", C: "INCORRECT: `maxRetries` limits the number of retries but doesn't solve the timing issue. Claude would still retry immediately and hit the rate limit each time.", D: "INCORRECT: `fallbackTool` is not a standard error response field, and suggesting tool alternatives should be communicated through the error message text if appropriate." }
  },
  {
    id: "d2-033",
    domain: "D2",
    taskStatement: "2.2",
    topic: "Structured Error Responses",
    difficulty: "hard",
    scenario: 1,
    importance: "Business rule violations need user-facing context",
    question: "A support agent tries to apply a 50% discount via `apply_discount(order_id, percentage: 50)`. The tool returns: `{ isError: true, content: [{ type: 'text', text: 'BUSINESS_RULE_ERR: max_discount_exceeded, limit=30, requested=50' }] }`. Claude tells the customer: 'BUSINESS_RULE_ERR: max_discount_exceeded'. What response redesign fixes this?",
    choices: { A: "Return `isError: false` with the error in a separate metadata field Claude won't display, leveraging the native capabilities of the platform through the standard interface layer", B: "Remove the error code from the message and just return 'Discount failed', following established patterns for this architecture for production-grade reliability", C: "Add a system prompt rule telling Claude to never show error codes to customers, with clear separation between policy and implementation with proper scope boundaries", D: "Return separate fields: `{ isError: true, content: [{ type: 'text', text: 'The maximum allowed discount is 30%. Would you like me to apply a 30% discount instead?' }], _meta: { errorCode: 'MAX_DISCOUNT_EXCEEDED', maxAllowed: 30, requested: 50 } }`" },
    correct: "D",
    explanations: { A: "INCORRECT: Setting `isError: false` makes Claude treat the response as a success, which is semantically wrong and could lead to Claude confirming a discount that wasn't applied.", B: "INCORRECT: 'Discount failed' is too vague — Claude can't help the customer understand why or what alternatives exist. Error messages must be informative and actionable.", C: "INCORRECT: System prompt rules about formatting are fragile. The tool response itself should be designed so that its text content is safe and helpful to surface directly.", D: "CORRECT: The content text should be customer-friendly and actionable, while structured metadata can carry machine-readable error details. This lets Claude present the friendly message directly while having the technical details for logging or decision-making. The suggestion to apply 30% gives Claude a concrete alternative to offer." }
  },
  {
    id: "d2-034",
    domain: "D2",
    taskStatement: "2.2",
    topic: "Structured Error Responses",
    difficulty: "hard",
    scenario: 3,
    importance: "Concurrent tool errors need independent handling",
    question: "A research system calls 3 tools in parallel: `fetch_sec_filings`, `fetch_earnings_calls`, and `fetch_analyst_reports`. Two succeed but `fetch_analyst_reports` returns a permission error. Claude currently treats the entire batch as failed. What error handling pattern should be implemented?",
    choices: { A: "Serial execution instead of parallel to isolate errors, which maintains reliability under production load across all affected components", B: "Each tool should return its result independently so Claude can process successful results and handle the single error separately — presenting available data while noting the unavailable source", C: "Add a `batch_tool_call` wrapper that aggregates results and errors into a single response, integrated into the standard development pipeline according to the configured policy", D: "Wrap all parallel calls in a single try-catch and return either all results or a single error, using the standard configuration for this scenario according to the configured policy" },
    correct: "B",
    explanations: { A: "INCORRECT: Serial execution eliminates the latency benefit of parallel calls and doesn't improve error handling. The same permission error would occur serially.", B: "CORRECT: Independent tool responses let Claude process each result on its own merits. It can present SEC filings and earnings call data while telling the researcher that analyst reports are unavailable due to a permission issue. This maximizes the value of successful calls.", C: "INCORRECT: A batch wrapper that merges results loses the per-tool error granularity and adds unnecessary complexity. Claude natively handles multiple tool results independently.", D: "INCORRECT: All-or-nothing error handling discards successful results when one tool fails. In research contexts, partial data is often highly valuable." }
  },
  {
    id: "d2-035",
    domain: "D2",
    taskStatement: "2.2",
    topic: "Structured Error Responses",
    difficulty: "medium",
    scenario: 4,
    importance: "Error recovery suggestions should be specific",
    question: "A developer agent calls `run_tests(suite: 'integration')` and 3 out of 50 tests fail. The tool returns the failures but with `isError: true`. Claude reports 'Tests failed' and stops helping. What is the better error response design for partial test failures?",
    choices: { A: "Return `isError: false` with only passing tests to keep the output positive, ensuring consistent behavior across all sessions with explicit dependency management", B: "Return `isError: false` with full results: passed count, failed count, and detailed failure messages for each failed test, allowing Claude to analyze the specific failures and suggest fixes", C: "Return `isError: true` with all 50 test results for completeness, following established patterns for this architecture with explicit dependency management", D: "Return `isError: true` with just the failure count to keep the response small, with explicit handling of edge cases and boundaries for production-grade reliability" },
    correct: "B",
    explanations: { A: "INCORRECT: Hiding failures is dangerous — the developer needs to know about failing tests. Suppressing negative results undermines trust and utility.", B: "CORRECT: Partial test failures are not an error in the tool invocation — the tool worked correctly. `isError: false` is appropriate because the tool successfully executed. The response should include both the success summary and detailed failure information so Claude can analyze specific failures and suggest fixes.", C: "INCORRECT: `isError: true` is semantically wrong for successful tool execution with some test failures. Also, returning all 50 results when only 3 failed wastes context space.", D: "INCORRECT: `isError: true` signals the tool itself failed, not that some tests failed. Also, omitting failure details prevents Claude from helping debug the specific issues." }
  },
  {
    id: "d2-036",
    domain: "D2",
    taskStatement: "2.2",
    topic: "Structured Error Responses",
    difficulty: "hard",
    scenario: 1,
    importance: "Sensitive error data must not leak to end users",
    question: "A support agent's `verify_identity` tool fails and returns: `{ isError: true, content: [{ type: 'text', text: 'Identity check failed. SSN partial match: last 4 digits 7842 matched but full SSN 435-82-7842 has fraud flag from TransUnion report TU-2024-8891.' }] }`. What is the critical problem?",
    choices: { A: "The `isError: true` flag prevents Claude from using the partial match information, leveraging the native capabilities of the platform", B: "The error should include `isRetryable: true` since identity verification can be retried per the architectural requirements", C: "The error response leaks sensitive PII (full SSN) and internal system references that Claude might expose to the customer in its response", D: "The error message is too long for Claude to process efficiently, applied systematically across the entire system with proper scope boundaries" },
    correct: "C",
    explanations: { A: "INCORRECT: While `isError: true` affects how Claude handles the response, the critical issue is the PII exposure, not the error flag.", B: "INCORRECT: While retryability might be relevant, the immediate critical issue is the PII leak. Retrying with the same flagged SSN would fail again anyway.", C: "CORRECT: The error response contains a full SSN and an internal fraud report reference. Since Claude may incorporate tool response content into its customer-facing reply, this creates a severe data leak risk. Error responses must be sanitized of PII and internal identifiers before reaching Claude. The tool should return: 'Identity verification failed. The customer should be transferred to the fraud prevention team.'", D: "INCORRECT: Message length is not the issue. Claude can process long messages fine." }
  },

  // ============================================================
  // Task 2.3: Tool Distribution & tool_choice (18 questions)
  // ============================================================
  {
    id: "d2-037",
    domain: "D2",
    taskStatement: "2.3",
    topic: "Tool Distribution & tool_choice",
    difficulty: "hard",
    scenario: 3,
    importance: "Tool count degradation threshold",
    question: "A multi-agent research system has a 'general analyst' agent with 22 tools spanning market data, document analysis, financial modeling, compliance checks, and report generation. Accuracy has dropped 25% since the tool set grew past 15. What is the best mitigation?",
    choices: { A: "Add more detailed descriptions to all 22 tools to improve selection accuracy, which addresses the core requirements effectively to maintain system integrity", B: "Use `tool_choice: any` to force the agent to always use a tool, reducing skipped-tool errors, scoped to the relevant context for this operation according to the configured policy", C: "Keep all 22 tools but add a routing system prompt that maps query types to specific tools, supporting both automated and manual processes across all affected components", D: "Split into specialized sub-agents — a market data agent (5 tools), a document analysis agent (4 tools), a modeling agent (4 tools), a compliance agent (4 tools) — each with focused tool sets under the selection degradation threshold" },
    correct: "D",
    explanations: { A: "INCORRECT: Better descriptions help marginally but cannot overcome the fundamental degradation that occurs beyond ~15-18 tools. Selection accuracy drops because Claude must evaluate too many options, regardless of description quality.", B: "INCORRECT: `tool_choice: any` forces a tool call but doesn't improve selection quality. It actually makes things worse because Claude must call a tool even when none is appropriate, leading to misuse.", C: "INCORRECT: Routing instructions for 22 tools in the system prompt would be extremely long and create competing ghost associations. System prompt routing doesn't scale and degrades with conversation length.", D: "CORRECT: Splitting into specialized sub-agents with 4-5 focused tools each keeps each agent well under the degradation threshold. This is the recommended architectural pattern — specialized agents with focused tool sets perform dramatically better than generalist agents with sprawling tool sets." }
  },
  {
    id: "d2-038",
    domain: "D2",
    taskStatement: "2.3",
    topic: "Tool Distribution & tool_choice",
    difficulty: "medium",
    scenario: 1,
    importance: "auto vs any vs forced tool_choice",
    question: "A customer support agent should ALWAYS look up the customer's account before responding — even for simple greetings. Currently with `tool_choice: 'auto'`, Claude sometimes skips the lookup and responds directly. What is the correct fix?",
    choices: { A: "Change to `tool_choice: { type: 'tool', name: 'lookup_customer' }` for the first turn to force the specific lookup, then switch to `auto` for subsequent turns", B: "Add a system prompt instruction: 'Always look up the customer before responding', maintaining compatibility with existing workflows while preserving existing behavior", C: "Change to `tool_choice: { type: 'any' }` to force Claude to call a tool on every turn, which maintains reliability under production load", D: "Add a pre-processing step that calls `lookup_customer` before invoking Claude, with clear separation between policy and implementation for production-grade reliability" },
    correct: "A",
    explanations: { A: "CORRECT: Using forced tool choice (`type: 'tool', name: 'lookup_customer'`) on the first turn guarantees the specific lookup happens. Switching to `auto` for subsequent turns gives Claude flexibility for the rest of the conversation. This pattern of forced-first-then-auto is common for mandatory initialization steps.", B: "INCORRECT: System prompt instructions are advisory and can be ignored by Claude, especially for simple queries where it deems a lookup unnecessary. This is why `tool_choice` enforcement exists.", C: "INCORRECT: `any` forces a tool call but doesn't specify WHICH tool. Claude might call a different tool instead of `lookup_customer`. Also, forcing a tool call on every turn (including follow-ups) adds unnecessary latency.", D: "INCORRECT: While pre-processing works, it removes Claude from the loop and wastes a call if the customer provides their info in the message. The `tool_choice` mechanism is the designed solution for this pattern." }
  },
  {
    id: "d2-039",
    domain: "D2",
    taskStatement: "2.3",
    topic: "Tool Distribution & tool_choice",
    difficulty: "hard",
    scenario: 3,
    importance: "Out-of-specialization tools cause active misuse",
    question: "A research system's 'literature review' agent has tools for searching papers and synthesizing findings. The team adds a `execute_trade` tool to this agent so analysts can act on insights immediately. What is the most likely failure mode?",
    choices: { A: "The trade tool will not work because it's in the wrong agent, following established patterns for this architecture with proper scope boundaries", B: "Claude will ignore the trade tool entirely since it doesn't match the literature review context, supporting both automated and manual processes per the architectural requirements", C: "Claude will occasionally invoke `execute_trade` inappropriately — for example, interpreting a paper's recommendation as an instruction to trade — because the tool is outside the agent's specialization and its presence creates an active misuse risk", D: "The trade tool will slow down the literature review by consuming context window space, using structured data for all inter-component communication through the standard interface layer" },
    correct: "C",
    explanations: { A: "INCORRECT: The trade tool will technically work if called — the problem isn't functionality but inappropriate invocation.", B: "INCORRECT: Claude does NOT ignore available tools. It evaluates all tools for potential relevance, and an out-of-specialization tool may get selected when language in the conversation coincidentally matches its description.", C: "CORRECT: Out-of-specialization tools don't just sit unused — they create active misuse risk. When a research paper says 'we recommend increasing allocation to emerging markets', Claude might interpret this as a trade instruction and invoke `execute_trade`. This is far worse than confusion — it's dangerous action in the wrong context.", D: "INCORRECT: A single extra tool's schema is minimal context overhead. The performance concern is about selection quality, not context space." }
  },
  {
    id: "d2-040",
    domain: "D2",
    taskStatement: "2.3",
    topic: "Tool Distribution & tool_choice",
    difficulty: "medium",
    scenario: 4,
    importance: "tool_choice: none disables all tool use",
    question: "A developer productivity agent has tools for code search, test execution, and deployment. During a code review phase, the team wants Claude to only analyze and comment — no tool execution. How should this be implemented?",
    choices: { A: "Set `tool_choice: { type: 'auto' }` and trust Claude to know not to use tools during reviews according to the configured policy", B: "Set `tool_choice: { type: 'none' }` to disable tool use while keeping tools defined in the schema for Claude's reference", C: "Remove all tools from the agent configuration for code review sessions, aligned with the documented best practices", D: "Add a system prompt instruction: 'During code review, do not use any tools' with explicit dependency management" },
    correct: "B",
    explanations: { A: "INCORRECT: `auto` allows tool use at Claude's discretion. During code reviews, Claude might call code search to verify references or run tests to check its hypotheses, which defeats the review-only intent.", B: "CORRECT: `tool_choice: { type: 'none' }` tells Claude that tool use is disabled for this request. Claude will not attempt to call any tools but retains awareness of the tool schema for context. This is the clean, API-level mechanism for the code-review-only mode.", C: "INCORRECT: Removing tools from the configuration means Claude loses awareness of what tools exist, which may be useful context for its review (e.g., knowing tests can be run). `tool_choice: none` preserves awareness while preventing invocation.", D: "INCORRECT: System prompt instructions are advisory. Claude might still call tools if it deems them useful, especially if the review reveals a bug it wants to investigate. `tool_choice: none` is an enforcement mechanism, not a suggestion." }
  },
  {
    id: "d2-041",
    domain: "D2",
    taskStatement: "2.3",
    topic: "Tool Distribution & tool_choice",
    difficulty: "hard",
    scenario: 1,
    importance: "Scoped cross-role tools for high-frequency needs only",
    question: "A customer support system has specialized agents: billing, technical, and shipping. All three agents frequently need to look up customer profiles. Currently, the `lookup_customer` tool exists only in the billing agent, requiring expensive cross-agent routing for every technical or shipping query. What is the correct tool distribution approach?",
    choices: { A: "Merge all three agents into one general support agent to avoid cross-agent communication, with clear separation between policy and implementation", B: "Keep `lookup_customer` only in the billing agent and optimize the cross-agent routing latency for production-grade reliability", C: "Duplicate `lookup_customer` into all three agents as a scoped cross-role tool, since customer lookup is a high-frequency need across all specializations", D: "Create a separate 'customer profile' agent that all other agents query, maintaining compatibility with existing workflows for production-grade reliability" },
    correct: "C",
    explanations: { A: "INCORRECT: Merging agents loses the specialization benefit. Each agent has domain-specific tools (billing tools, technical tools, shipping tools) that would create tool sprawl if combined.", B: "INCORRECT: Optimizing cross-agent routing still adds latency and complexity for a high-frequency operation. When a tool is needed by all agents regularly, local access is better.", C: "CORRECT: High-frequency cross-role tools should be scoped (duplicated) into each agent that needs them. Customer lookup is a foundational operation that all support agents need on nearly every interaction. Duplicating this tool eliminates cross-agent overhead while keeping each agent's tool set focused.", D: "INCORRECT: A dedicated profile agent adds another layer of indirection. Every interaction now requires an extra agent-to-agent call for basic customer lookup, increasing latency and system complexity." }
  },
  {
    id: "d2-042",
    domain: "D2",
    taskStatement: "2.3",
    topic: "Tool Distribution & tool_choice",
    difficulty: "hard",
    scenario: 3,
    importance: "Dynamic tool set based on conversation phase",
    question: "A multi-agent research system has distinct phases: data gathering, analysis, and report generation. During data gathering, Claude has access to 12 search and retrieval tools. During analysis, it needs 6 statistical tools. During reporting, it needs 4 formatting tools. What is the optimal tool distribution strategy?",
    choices: { A: "Provide all 22 tools at all times so Claude has maximum flexibility, with proper validation at each stage per the architectural requirements", B: "Provide all 22 tools but use system prompt instructions to indicate which tools are appropriate for each phase to maintain system integrity", C: "Create 3 separate conversation threads, one per phase, and pass context between them, supporting both automated and manual processes through the standard interface layer", D: "Dynamically adjust the tool set per conversation phase — provide only the tools relevant to the current phase, keeping each phase's tool count under the degradation threshold" },
    correct: "D",
    explanations: { A: "INCORRECT: 22 tools exceeds the degradation threshold (~15-18), leading to poor selection accuracy throughout. Providing all tools also risks Claude using analysis tools during data gathering or search tools during report generation.", B: "INCORRECT: System prompt phase instructions are advisory and fragile. With 22 tools visible, Claude may still select out-of-phase tools, and the phase routing instructions consume valuable context.", C: "INCORRECT: Separate conversation threads lose the context from previous phases. The analysis phase needs awareness of what data was gathered, and reporting needs awareness of analysis results.", D: "CORRECT: Dynamically scoping tools to the current phase keeps each phase's tool count low (4-12), well within the effective range. This ensures optimal selection accuracy and prevents cross-phase tool misuse. The orchestrator manages tool set transitions based on the conversation's progression." }
  },
  {
    id: "d2-043",
    domain: "D2",
    taskStatement: "2.3",
    topic: "Tool Distribution & tool_choice",
    difficulty: "medium",
    scenario: 4,
    importance: "Forced tool_choice for structured output",
    question: "A developer productivity system needs Claude to output a structured code review in a specific JSON format. The system provides a `submit_review` tool with the exact schema. But Claude sometimes outputs the review as plain text instead of calling the tool. What is the fix?",
    choices: { A: "Add a system prompt instruction: 'Always output reviews using the submit_review tool'", B: "Add few-shot examples showing tool usage for review submission according to the configured policy", C: "Parse Claude's text output and convert it to the tool schema format with explicit dependency management", D: "Use `tool_choice: { type: 'tool', name: 'submit_review' }` to force Claude to call the specific tool" },
    correct: "D",
    explanations: { A: "INCORRECT: System prompt instructions are advisory. Claude may still output text directly, especially if it determines the review is simple enough to express in prose.", B: "INCORRECT: Few-shot examples improve consistency but don't guarantee tool use. Claude may still choose text output in some cases.", C: "INCORRECT: Post-processing text into structured format is fragile, error-prone, and requires maintaining a complex parser. The `tool_choice` mechanism eliminates this need.", D: "CORRECT: `tool_choice: { type: 'tool', name: 'submit_review' }` forces Claude to call that specific tool, guaranteeing structured output in the defined schema. This is the designed mechanism for ensuring structured output through tool use." }
  },
  {
    id: "d2-044",
    domain: "D2",
    taskStatement: "2.3",
    topic: "Tool Distribution & tool_choice",
    difficulty: "hard",
    scenario: 1,
    importance: "Tool overlap between agents causes routing confusion",
    question: "A customer support system has a 'billing agent' with `search_invoices(query)` and a 'general agent' with `search_all(query)`. The `search_all` tool can also find invoices. When a customer asks about an invoice, the routing layer sends the query to the general agent because `search_all` matches. But the billing agent's `search_invoices` provides better results with billing-specific filters. What is the root cause?",
    choices: { A: "The customer's query was ambiguous, with appropriate error handling at each step with proper scope boundaries", B: "The routing layer is using the wrong algorithm to select agents, with explicit handling of edge cases and boundaries", C: "The general agent's `search_all` tool has too broad a description, creating overlap with the billing agent's specialized tool", D: "The billing agent's `search_invoices` description isn't good enough, scoped to the relevant context for this operation" },
    correct: "C",
    explanations: { A: "INCORRECT: 'Tell me about invoice #12345' is not ambiguous. The issue is tool description overlap creating multiple valid routing targets.", B: "INCORRECT: The routing algorithm is working as designed — it matched the query to a tool that claims to handle it. The problem is the tool's broad claim, not the routing logic.", C: "CORRECT: A tool with an overly broad description like `search_all` creates overlap with specialized tools. The routing layer sees that `search_all` can handle invoice queries, so it routes to the general agent. The fix is to either narrow `search_all`'s description to exclude billing-specific queries or remove it in favor of domain-specific search tools.", D: "INCORRECT: Even a perfect `search_invoices` description doesn't help if the routing layer is diverting queries to the general agent before they reach the billing agent." }
  },
  {
    id: "d2-045",
    domain: "D2",
    taskStatement: "2.3",
    topic: "Tool Distribution & tool_choice",
    difficulty: "medium",
    scenario: 5,
    importance: "tool_choice in CI/CD pipelines",
    question: "In a CI/CD pipeline using Claude Code, a specific step requires Claude to run linting before any other action. The available tools include `run_linter`, `run_tests`, `deploy`, and `rollback`. What tool_choice configuration ensures linting happens first?",
    choices: { A: "`tool_choice: { type: 'tool', name: 'run_linter' }` for the first API call, then switch to `{ type: 'auto' }` for subsequent calls", B: "`tool_choice: { type: 'auto' }` with the linter listed first in the tools array using the established workflow conventions", C: "`tool_choice: { type: 'any' }` with a system prompt saying 'Run linter first', with explicit handling of edge cases and boundaries", D: "`tool_choice: { type: 'none' }` initially, then enable tools after linting completes externally according to the configured policy" },
    correct: "A",
    explanations: { A: "CORRECT: Forced tool choice for `run_linter` on the first call guarantees linting happens. Switching to `auto` afterward gives Claude flexibility to proceed based on linting results — running tests if clean, or addressing lint errors.", B: "INCORRECT: Tool array ordering does not influence Claude's selection priority. `auto` means Claude chooses freely, and it might skip linting if it deems other actions more relevant.", C: "INCORRECT: `any` forces a tool call but doesn't guarantee it's `run_linter`. Claude might call `run_tests` or `deploy` instead. System prompt instructions don't override the `any` behavior.", D: "INCORRECT: Running linting externally removes Claude from the process. Claude can't reason about lint results or decide next steps if it didn't invoke the linter." }
  },
  {
    id: "d2-046",
    domain: "D2",
    taskStatement: "2.3",
    topic: "Tool Distribution & tool_choice",
    difficulty: "hard",
    scenario: 3,
    importance: "Tool proliferation anti-pattern",
    question: "A research platform's tool set grew from 8 tools to 19 tools over a year. The team notices that Claude's tool selection accuracy dropped from 94% to 71%, but each individual tool works correctly. A junior engineer suggests adding detailed usage instructions for each tool in the system prompt. A senior engineer disagrees. Who is correct and why?",
    choices: { A: "The senior engineer is correct — the issue is tool count exceeding the degradation threshold, and system prompt instructions will create ghost associations that further degrade selection. The tools need consolidation.", B: "Neither is correct — the issue is that individual tool descriptions need to be improved, not tool count, scoped to the relevant context for this operation across all affected components", C: "Both are partially correct — add system prompt instructions AND consolidate tools simultaneously, leveraging the native capabilities of the platform with explicit dependency management", D: "The junior engineer is correct — detailed system prompt instructions will restore selection accuracy by providing explicit routing rules according to the configured policy" },
    correct: "A",
    explanations: { A: "CORRECT: 19 tools exceeds the ~15-18 tool degradation threshold where selection accuracy drops regardless of description quality. System prompt instructions for this many tools would compound the problem by creating ghost associations. The correct fix is tool consolidation — merging overlapping tools and eliminating redundancy to get back under the threshold.", B: "INCORRECT: While description quality matters, research shows that beyond the degradation threshold, even excellent descriptions can't maintain selection accuracy. Tool count must be reduced.", C: "INCORRECT: Adding system prompt instructions is actively harmful at this tool count. Only consolidation addresses the root cause.", D: "INCORRECT: Adding detailed routing instructions for 19 tools in the system prompt would create a massive set of competing ghost associations. Each keyword-to-tool mapping interferes with the description-based selection, likely making accuracy worse, not better." }
  },
  {
    id: "d2-047",
    domain: "D2",
    taskStatement: "2.3",
    topic: "Tool Distribution & tool_choice",
    difficulty: "hard",
    scenario: 4,
    importance: "Role-appropriate tool sets prevent misuse",
    question: "A developer productivity platform gives all developers the same Claude agent with 14 tools including `modify_production_config`, `delete_database`, and `revoke_access`. A junior developer asks Claude to 'clean up the test environment' and Claude calls `delete_database` against production because the tool description mentions 'cleaning up databases'. What architectural principle was violated?",
    choices: { A: "Claude should have asked for confirmation before executing destructive operations, ensuring consistent behavior across all sessions through the standard interface layer", B: "The principle of role-appropriate tool sets — different user roles should have different tool sets, with destructive production tools only available to senior/admin roles", C: "The system should have had a production/test environment flag in the tool parameters, leveraging the native capabilities of the platform using the established workflow conventions", D: "The tool descriptions should have been more specific about which environments they target, using the standard configuration for this scenario with proper scope boundaries" },
    correct: "B",
    explanations: { A: "INCORRECT: Confirmation steps are defense-in-depth but not sufficient as the primary control. If the tool is available, Claude might be convinced to skip confirmation through prompt injection or conversational pressure.", B: "CORRECT: Role-appropriate tool sets mean that junior developers should never have access to `delete_database`, `modify_production_config`, or `revoke_access`. These tools should only appear in agents assigned to senior engineers or admins. This prevents the possibility of misuse — you can't misuse a tool that doesn't exist in your agent.", C: "INCORRECT: An environment flag is useful but still relies on Claude selecting the correct flag value. Removing the production tool entirely from junior agents is more robust.", D: "INCORRECT: While better descriptions help, they don't prevent the fundamental risk. A determined or confused Claude might still call a production tool. The fix is removing the tool from the junior developer's agent entirely." }
  },
  {
    id: "d2-048",
    domain: "D2",
    taskStatement: "2.3",
    topic: "Tool Distribution & tool_choice",
    difficulty: "medium",
    scenario: 6,
    importance: "any tool_choice for mandatory tool use",
    question: "A structured data extraction system should ALWAYS use tools to extract data — never respond with unstructured text. What tool_choice setting enforces this?",
    choices: { A: "`tool_choice: { type: 'auto' }` with a system prompt saying 'Always use tools'", B: "`tool_choice: { type: 'tool', name: 'extract_data' }` to force the specific extraction tool", C: "`tool_choice: { type: 'none' }` initially to let Claude plan, then switch to `any`", D: "`tool_choice: { type: 'any' }` which forces Claude to call at least one tool on every turn" },
    correct: "D",
    explanations: { A: "INCORRECT: `auto` allows Claude to skip tool use entirely if it decides to respond with text. System prompt instructions are advisory and don't enforce tool use.", B: "INCORRECT: While this works if there's only one extraction tool, it's too restrictive if the system has multiple extraction tools for different document types. `any` allows Claude to choose the appropriate extraction tool while still guaranteeing a tool is called.", C: "INCORRECT: Starting with `none` means Claude's first response has no tool output. In an extraction pipeline, every response should produce structured data.", D: "CORRECT: `tool_choice: { type: 'any' }` forces Claude to call at least one tool on every response. This is appropriate for extraction pipelines where the output must always be structured data from a tool, never free-text responses." }
  },
  {
    id: "d2-049",
    domain: "D2",
    taskStatement: "2.3",
    topic: "Tool Distribution & tool_choice",
    difficulty: "hard",
    scenario: 1,
    importance: "Tool set changes mid-conversation",
    question: "A customer support conversation starts with a billing inquiry (billing tools available). Midway through, the customer pivots to a technical issue. The orchestrator dynamically swaps the billing tools for technical tools. Claude's next response tries to call `check_billing_status` (now removed) and fails. What is the correct approach?",
    choices: { A: "Use `tool_choice: none` for one turn to force a text-only transition response, then provide the new tools to maintain system integrity", B: "Keep all tools available at all times to prevent this issue, using structured data for all inter-component communication with proper scope boundaries", C: "Never change tools mid-conversation — start a new conversation for the technical issue, maintaining compatibility with existing workflows", D: "When swapping tool sets mid-conversation, include a system message informing Claude that the available tools have changed and listing the new tool set" },
    correct: "D",
    explanations: { A: "INCORRECT: Forcing a text-only response adds latency and may produce an awkward transition message. Claude can handle tool set changes in a single turn if properly informed.", B: "INCORRECT: Keeping all tools available creates tool sprawl and the selection degradation problems discussed in other questions. Dynamic tool sets are valuable.", C: "INCORRECT: Starting a new conversation loses the customer context. The customer's billing issue context may be relevant to the technical issue they're now describing.", D: "CORRECT: When tool sets change mid-conversation, Claude needs explicit notification via a system message. Without this, Claude's cached understanding of available tools is stale, and it may try to call tools that no longer exist. The system message should list what tools are now available and optionally explain why the change occurred." }
  },
  {
    id: "d2-050",
    domain: "D2",
    taskStatement: "2.3",
    topic: "Tool Distribution & tool_choice",
    difficulty: "hard",
    scenario: 3,
    importance: "Parallel tool calling constraints",
    question: "A research system allows Claude to call tools in parallel for efficiency. Claude calls `fetch_sec_filings(ticker: 'AAPL')` and `fetch_sec_filings(ticker: 'MSFT')` in parallel — this works well. But it also calls `create_report(data: ...)` in parallel with the fetches, using placeholder data because the real data hasn't returned yet. What constraint should be applied?",
    choices: { A: "Disable parallel tool calling entirely to ensure sequential execution, maintaining compatibility with existing workflows per the architectural requirements", B: "Make `create_report` validate its input and return an error if data looks like a placeholder, maintaining compatibility with existing workflows across all affected components", C: "Add dependency metadata to tools so the orchestrator knows `create_report` depends on `fetch_sec_filings` results, and enforce that dependent tools execute sequentially while independent tools can parallelize", D: "Add a system prompt instruction: 'Never call create_report until all data is fetched', which addresses the core requirements effectively with proper scope boundaries" },
    correct: "C",
    explanations: { A: "INCORRECT: Disabling all parallel calls eliminates the performance benefit for independent calls (like fetching data for multiple tickers simultaneously). The constraint should target dependencies, not all parallelism.", B: "INCORRECT: Input validation in `create_report` catches the error after the fact but wastes a tool call and adds latency. Prevention through dependency metadata is better than detection.", C: "CORRECT: Tool dependency metadata allows the orchestrator to enforce execution order where needed while preserving parallelism for independent operations. Independent fetches can run in parallel, but `create_report` waits for all fetches to complete. This is the best of both worlds.", D: "INCORRECT: System prompt instructions about execution order are advisory and cannot be enforced. Claude might still attempt to call dependent tools in parallel if it predicts the data." }
  },
  {
    id: "d2-051",
    domain: "D2",
    taskStatement: "2.3",
    topic: "Tool Distribution & tool_choice",
    difficulty: "medium",
    scenario: 5,
    importance: "CI/CD tool distribution by pipeline stage",
    question: "A CI/CD pipeline using Claude Code has three stages: build, test, deploy. The `deploy_production` tool should only be available during the deploy stage, after tests pass. What prevents premature deployment?",
    choices: { A: "Use `tool_choice: { type: 'tool', name: 'deploy_production' }` only in the deploy stage through the standard interface layer", B: "Add a `tests_passed: boolean` required parameter to `deploy_production`, using structured data for all inter-component communication", C: "Include `deploy_production` in all stages but add a description: 'Only use after all tests pass' with proper scope boundaries", D: "Only include `deploy_production` in the tool set for the deploy stage, which the orchestrator only activates after the test stage succeeds" },
    correct: "D",
    explanations: { A: "INCORRECT: Forced tool choice during deploy stage would require Claude to deploy on every turn, which isn't correct. The deploy stage may involve pre-deployment checks before the actual deployment call.", B: "INCORRECT: A boolean parameter relies on Claude setting it correctly. Claude might set `tests_passed: true` based on its assessment rather than actual test results. Stage-gated tool availability is more robust.", C: "INCORRECT: Description-based restrictions are advisory. Claude might call `deploy_production` during the build stage if it determines tests are likely to pass. This is unacceptably risky for production deployments.", D: "CORRECT: The safest approach is to not include `deploy_production` in the tool set until the deploy stage. If the tool doesn't exist in Claude's available tools, it cannot be called. The orchestrator gates tool availability based on pipeline progress." }
  },
  {
    id: "d2-052",
    domain: "D2",
    taskStatement: "2.3",
    topic: "Tool Distribution & tool_choice",
    difficulty: "hard",
    scenario: 4,
    importance: "Cost optimization through tool_choice strategy",
    question: "A developer productivity system processes 10,000 support requests daily. 60% are simple FAQ questions that don't need tool calls, but Claude with `tool_choice: 'auto'` still calls `search_docs` for 40% of these simple questions, wasting API credits on unnecessary tool calls. However, the other 40% of complex questions critically need tool access. What is the most cost-effective approach?",
    choices: { A: "Remove all tools and use a RAG-only approach for knowledge retrieval, providing a deterministic approach to the workflow for production-grade reliability", B: "Use `tool_choice: 'any'` to ensure every tool call is intentional, using structured data for all inter-component communication according to the configured policy", C: "Use `tool_choice: 'none'` for all requests to eliminate unnecessary tool calls, and handle complex questions separately, maintaining compatibility with existing workflows per the architectural requirements", D: "Implement a lightweight classifier that routes simple FAQs with `tool_choice: 'none'` and complex questions with `tool_choice: 'auto'`, reducing unnecessary tool calls while preserving tool access when needed" },
    correct: "D",
    explanations: { A: "INCORRECT: RAG-only removes the ability to take actions (create tickets, execute commands) that complex questions require. Tools are essential for the action-oriented 40% of queries.", B: "INCORRECT: `any` forces a tool call on EVERY request, making the problem worse — now 100% of simple FAQs waste tool calls instead of 40%.", C: "INCORRECT: Using `none` for all requests means complex questions that need tool access can't use tools. This breaks functionality for 40% of requests.", D: "CORRECT: A lightweight pre-classifier (which can be a simple keyword/pattern matcher or a fast Claude Haiku call) routes simple FAQs with `tool_choice: 'none'` and complex questions with `tool_choice: 'auto'`. This eliminates unnecessary tool calls for simple questions while preserving full tool access for complex ones. At 10,000 daily requests, this saves significant cost." }
  },
  {
    id: "d2-053",
    domain: "D2",
    taskStatement: "2.3",
    topic: "Tool Distribution & tool_choice",
    difficulty: "medium",
    scenario: 3,
    importance: "Preventing tool_choice misuse",
    question: "A research system forces `tool_choice: { type: 'tool', name: 'search_database' }` on every turn to ensure Claude always searches before responding. Researchers complain that Claude calls `search_database` with empty or meaningless queries when it already has enough information to respond. What is the problem?",
    choices: { A: "Claude needs more context about when searching is appropriate, with explicit handling of edge cases and boundaries using the established workflow conventions", B: "The tool description doesn't explain when searches are necessary, with explicit handling of edge cases and boundaries for production-grade reliability", C: "Forced tool choice on every turn requires Claude to call the tool even when unnecessary, leading to wasteful or meaningless calls. The system should use forced choice only for the initial search, then switch to `auto`.", D: "The `search_database` tool has a bug that doesn't handle empty queries, which maintains reliability under production load with explicit dependency management" },
    correct: "C",
    explanations: { A: "INCORRECT: Claude may know exactly when searching is appropriate, but forced tool_choice overrides its judgment. The issue is the configuration, not Claude's understanding.", B: "INCORRECT: The description doesn't matter when tool_choice is forced — Claude must call the tool regardless of what the description says.", C: "CORRECT: Forced tool choice means Claude MUST call the named tool on every turn, regardless of whether it's needed. This leads to degenerate calls when Claude has enough information. The pattern should be forced choice for the initial turn (to ensure research happens) then `auto` for follow-ups (letting Claude decide if more searching is needed).", D: "INCORRECT: The tool handling empty queries is a symptom. The root cause is that Claude is forced to call the tool even when it has no meaningful query." }
  },
  {
    id: "d2-054",
    domain: "D2",
    taskStatement: "2.3",
    topic: "Tool Distribution & tool_choice",
    difficulty: "hard",
    scenario: 1,
    importance: "Agent handoff tool design",
    question: "A customer support system has billing, technical, and shipping agents. When a customer's issue spans domains (e.g., 'I was charged twice and my package arrived damaged'), the billing agent handles the charge but can't address the shipping issue. What tool distribution pattern handles multi-domain issues?",
    choices: { A: "Create a 'super agent' with all tools that handles multi-domain issues, scoped to the relevant context for this operation through the standard interface layer", B: "Add a scoped `handoff_to_agent(target: string, context: string)` tool to each agent, allowing agents to transfer conversations with full context to the appropriate specialist", C: "Give each agent all tools from all domains so any agent can handle any issue, with proper validation at each stage with explicit dependency management", D: "Tell customers to open separate tickets for each domain, which maintains reliability under production load through the standard interface layer" },
    correct: "B",
    explanations: { A: "INCORRECT: A super agent combines all domain tools, creating massive tool sprawl and degraded selection. It's the anti-pattern that specialization was designed to prevent.", B: "CORRECT: A `handoff_to_agent` tool is a lightweight cross-role tool that enables multi-domain handling without bloating tool sets. The billing agent handles the charge, then hands off to shipping with context about what was resolved. Each agent stays focused with its specialized tools.", C: "INCORRECT: Giving all tools to all agents creates tool sprawl and defeats the purpose of specialization. Each agent would have 15+ tools, exceeding the degradation threshold.", D: "INCORRECT: This creates a terrible customer experience. Multi-domain issues should be handled seamlessly through agent collaboration, not pushed back to the customer." }
  },

  // ============================================================
  // Task 2.4: MCP Integration (18 questions)
  // ============================================================
  {
    id: "d2-055",
    domain: "D2",
    taskStatement: "2.4",
    topic: "MCP Integration",
    difficulty: "hard",
    scenario: 4,
    importance: ".mcp.json vs ~/.claude.json configuration scoping",
    question: "A developer productivity team uses Claude Code across 12 projects. Each project needs access to a shared Jira MCP server, but the API tokens differ per project because each has a separate Jira project key. Additionally, every developer needs access to a personal notes MCP server regardless of project. Where should each configuration go?",
    choices: { A: "Put both in `~/.claude.json` since it applies everywhere, following established patterns for this architecture with proper scope boundaries", B: "Put the Jira server in `~/.claude.json` and the personal notes server in `.mcp.json`, using structured data for all inter-component communication for production-grade reliability", C: "Put both configurations in each project's `.mcp.json` file, using the standard configuration for this scenario across all affected components", D: "Put the Jira server (with project-specific tokens via `${JIRA_TOKEN}` env vars) in each project's `.mcp.json` and the personal notes server in each developer's `~/.claude.json`" },
    correct: "D",
    explanations: { A: "INCORRECT: Putting project-specific Jira configs in `~/.claude.json` means the same config applies to all 12 projects. Since each project has a different Jira project key and token, this doesn't work without complex environment variable switching.", B: "INCORRECT: This is backwards. Jira is project-specific (belongs in `.mcp.json`), and personal notes is user-specific (belongs in `~/.claude.json`).", C: "INCORRECT: Putting the personal notes server in every project's `.mcp.json` means it must be duplicated across all 12 projects and updated in 12 places if the configuration changes. Personal/global tools belong in `~/.claude.json`.", D: "CORRECT: `.mcp.json` is for project/shared configurations — the Jira server with project-specific tokens is project-scoped. `~/.claude.json` is for user/personal configurations — the personal notes server is user-scoped and should be available across all projects. Using `${JIRA_TOKEN}` env vars keeps credentials out of the shared config file." }
  },
  {
    id: "d2-056",
    domain: "D2",
    taskStatement: "2.4",
    topic: "MCP Integration",
    difficulty: "hard",
    scenario: 4,
    importance: "Never hardcode credentials in MCP config",
    question: "A developer is configuring an MCP server for a database connection in `.mcp.json`. They write: `{ \"mcpServers\": { \"database\": { \"command\": \"npx\", \"args\": [\"@db/mcp-server\"], \"env\": { \"DB_PASSWORD\": \"s3cureP@ss!\" } } } }`. This file is committed to the team's git repository. What is the security issue and correct fix?",
    choices: { A: "Encrypt the password in the config file using base64 encoding, leveraging the native capabilities of the platform with proper scope boundaries", B: "The password is too simple — use a stronger password and it's fine to commit, maintaining compatibility with existing workflows with proper scope boundaries", C: "The credential is hardcoded in a shared config file. Use environment variable expansion: `\"DB_PASSWORD\": \"${DB_PASSWORD}\"` so the credential is resolved from each developer's local environment at runtime, never stored in the repo", D: "Move the entire MCP configuration to `~/.claude.json` to keep it out of the repo, scoped to the relevant context for this operation using the established workflow conventions" },
    correct: "C",
    explanations: { A: "INCORRECT: Base64 is not encryption — it's a trivially reversible encoding. Anyone who can read the file can decode the password. This provides zero security.", B: "INCORRECT: Password strength is irrelevant when the credential is committed to a git repository. Any hardcoded credential in version control is a security vulnerability, regardless of complexity.", C: "CORRECT: MCP configuration supports `${VARIABLE}` syntax for environment variable expansion. The credential is resolved from each developer's local environment at runtime and never appears in the shared configuration file. This is the correct pattern for credentials in `.mcp.json`.", D: "INCORRECT: Moving to `~/.claude.json` keeps it out of the repo but loses the project-scoping benefit. It also doesn't solve the hardcoding problem — the credential would just be hardcoded in a different file." }
  },
  {
    id: "d2-057",
    domain: "D2",
    taskStatement: "2.4",
    topic: "MCP Integration",
    difficulty: "medium",
    scenario: 3,
    importance: "All MCP servers discovered at connection time",
    question: "A multi-agent research system configures 8 MCP servers in `.mcp.json`. The team notices that Claude Code startup takes 45 seconds because one MCP server (a custom internal tool) takes 30 seconds to initialize. They want Claude to start working immediately and discover that server later. Is this possible?",
    choices: { A: "No — all MCP servers configured in `.mcp.json` are discovered and connected at startup time. There is no lazy loading. The fix is to optimize the slow server's startup time.", B: "Yes — move the slow server to `~/.claude.json` so it loads after project servers, integrated into the standard development pipeline with explicit dependency management", C: "Yes — configure the slow server as a 'lazy' MCP server that connects only when its tools are first needed across all affected components", D: "Yes — add a `defer: true` flag to the slow server's configuration, using the standard configuration for this scenario for production-grade reliability" },
    correct: "A",
    explanations: { A: "CORRECT: All MCP servers are discovered and connected at startup. There is no lazy loading mechanism. If a server is slow to initialize, it blocks the entire startup. The solution is to optimize the server itself (caching, faster initialization) or run it as a persistent background process.", B: "INCORRECT: Moving to `~/.claude.json` doesn't change the loading behavior — it's still discovered at startup. The loading location doesn't affect timing.", C: "INCORRECT: MCP does not support lazy loading of servers. There is no 'lazy' connection mode — all configured servers are connected at discovery time.", D: "INCORRECT: There is no `defer: true` flag in MCP server configuration. This option does not exist in the protocol." }
  },
  {
    id: "d2-058",
    domain: "D2",
    taskStatement: "2.4",
    topic: "MCP Integration",
    difficulty: "hard",
    scenario: 3,
    importance: "Resources vs Tools distinction in MCP",
    question: "A research team is building an MCP server for their internal document repository. They need to decide between exposing documents as MCP Resources vs. MCP Tools. The documents are reference materials that Claude should be able to browse and read. Occasionally, a researcher needs Claude to update a document's metadata. How should this be modeled?",
    choices: { A: "Expose documents as Resources (content catalogs for browsing and reading) and expose `update_metadata` as a Tool (an action that modifies state)", B: "Expose documents as Tools with a `read_only: true` flag and metadata updates as Tools with `read_only: false` per the architectural requirements", C: "Expose everything as Resources since they're all related to documents, using the standard configuration for this scenario to maintain system integrity", D: "Expose everything as Tools: `get_document`, `list_documents`, `update_metadata`, which maintains reliability under production load" },
    correct: "A",
    explanations: { A: "CORRECT: In MCP, Resources are content catalogs — they represent data that Claude can browse and read (like documents). Tools represent actions that perform operations or modify state (like updating metadata). This distinction is fundamental to MCP's design: Resources for reading, Tools for acting.", B: "INCORRECT: There is no `read_only` flag on MCP Tools. The Resource/Tool distinction is the MCP-native way to differentiate read access from write actions.", C: "INCORRECT: Resources cannot perform actions that modify state. `update_metadata` changes data and must be a Tool. Resources are read-only content catalogs.", D: "INCORRECT: Exposing read-only document access as Tools forces Claude to make explicit tool calls just to browse content, adding latency. Resources are the correct abstraction for content that Claude should be able to browse." }
  },
  {
    id: "d2-059",
    domain: "D2",
    taskStatement: "2.4",
    topic: "MCP Integration",
    difficulty: "medium",
    scenario: 4,
    importance: "Community servers preferred for standard integrations",
    question: "A developer productivity team needs Claude Code to integrate with GitHub, Slack, and PostgreSQL. A senior engineer proposes building custom MCP servers for each. What is the recommended approach?",
    choices: { A: "Use community/official MCP servers for all three standard integrations, only building custom servers if the community versions lack critical functionality", B: "Use community servers for GitHub and Slack but build a custom PostgreSQL server for security reasons through the standard interface layer", C: "Build custom servers for all three to avoid dependency on external maintainers, using structured data for all inter-component communication", D: "Build custom MCP servers for all three to ensure they exactly match the team's requirements, with explicit handling of edge cases and boundaries" },
    correct: "A",
    explanations: { A: "CORRECT: Community MCP servers should be the first choice for standard integrations. They're battle-tested, regularly updated, and cover common use cases. Building custom servers is only justified when community versions lack specific critical functionality the team needs.", B: "INCORRECT: Community PostgreSQL MCP servers support standard security practices (connection strings via env vars, SSL). There's no inherent security advantage to a custom build unless the team has highly unusual security requirements.", C: "INCORRECT: Building three custom servers creates a maintenance burden far greater than the risk of depending on well-maintained community projects. The team would need to track API changes, fix bugs, and add features for all three.", D: "INCORRECT: Building custom servers for standard integrations duplicates significant effort. Official/community MCP servers for GitHub, Slack, and PostgreSQL are well-maintained and cover standard use cases." }
  },
  {
    id: "d2-060",
    domain: "D2",
    taskStatement: "2.4",
    topic: "MCP Integration",
    difficulty: "hard",
    scenario: 4,
    importance: "Enhanced descriptions needed for MCP tools to compete with built-ins",
    question: "A team deploys an MCP server providing a `search_code` tool alongside Claude Code's built-in code search tools (Grep, Glob). Developers report that Claude almost never uses the MCP `search_code` tool, even when it would provide better results (e.g., semantic search across the codebase). What is the most likely cause and fix?",
    choices: { A: "Remove the built-in Grep and Glob tools to force Claude to use the MCP tool, integrated into the standard development pipeline for production-grade reliability", B: "Rename the MCP tool to something unrelated to code search so it doesn't compete, with proper validation at each stage according to the configured policy", C: "The MCP tool's description must be enhanced to clearly differentiate its capabilities from built-in tools. For example: 'Performs semantic code search using embeddings — use for conceptual queries like \"authentication flow\" where keyword search (Grep) would miss related code.'", D: "MCP tools are lower priority than built-in tools. There's no fix — built-ins always win., following the recommended implementation pattern while preserving existing behavior" },
    correct: "C",
    explanations: { A: "INCORRECT: Removing built-in tools eliminates valuable capabilities. Keyword search (Grep) and path matching (Glob) serve different use cases than semantic search. The goal is complementary tools, not replacement.", B: "INCORRECT: Renaming the tool to avoid competition obscures its purpose. The fix is better descriptions that explain the complementary relationship, not name obfuscation.", C: "CORRECT: MCP tools must have enhanced descriptions that clearly explain what they offer beyond built-in tools. Without explicit differentiation (e.g., 'semantic search' vs. 'keyword search'), Claude defaults to familiar built-in tools. The description should explain when the MCP tool is superior.", D: "INCORRECT: MCP tools are not inherently lower priority. However, built-in tools have well-established descriptions that Claude is familiar with. MCP tools need enhanced descriptions to differentiate themselves and compete for selection." }
  },
  {
    id: "d2-061",
    domain: "D2",
    taskStatement: "2.4",
    topic: "MCP Integration",
    difficulty: "hard",
    scenario: 1,
    importance: "MCP server architecture for multi-tenant support",
    question: "A customer support platform serves 50 enterprise clients, each with their own CRM system. The team needs MCP integration with each client's CRM. What is the scalable MCP architecture?",
    choices: { A: "Create 50 separate MCP server configurations, one per client, in `.mcp.json`, which maintains reliability under production load using the established workflow conventions", B: "Create one MCP server that accepts a `tenant_id` parameter and routes to the correct CRM based on the tenant context, with credentials resolved from environment variables per tenant", C: "Have each client host their own MCP server that the platform connects to, integrated into the standard development pipeline to maintain system integrity", D: "Create one MCP server per CRM type (Salesforce, HubSpot, etc.) and route based on client configuration, providing a deterministic approach to the workflow" },
    correct: "B",
    explanations: { A: "INCORRECT: 50 separate MCP server configurations would all be discovered at startup, creating massive initialization overhead and potentially exceeding practical limits.", B: "CORRECT: A single multi-tenant MCP server with tenant routing is the scalable pattern. The server accepts `tenant_id`, resolves the appropriate CRM credentials from environment or secrets management, and routes requests accordingly. This keeps the MCP configuration minimal while supporting all 50 clients.", C: "INCORRECT: Having clients host MCP servers creates operational complexity, security concerns (clients would need to maintain server infrastructure), and connectivity challenges.", D: "INCORRECT: Grouping by CRM type is better than per-client but still requires multiple server configurations and doesn't handle clients with the same CRM type but different credentials cleanly." }
  },
  {
    id: "d2-062",
    domain: "D2",
    taskStatement: "2.4",
    topic: "MCP Integration",
    difficulty: "medium",
    scenario: 5,
    importance: "MCP in CI/CD environments",
    question: "A CI/CD pipeline uses Claude Code with MCP servers configured in `.mcp.json`. In the CI environment, the MCP servers need different credentials than local development. How should this be handled?",
    choices: { A: "Use `${VARIABLE}` environment variable expansion in `.mcp.json` and set the appropriate environment variables in the CI pipeline configuration", B: "Create a separate `.mcp.json` for CI with hardcoded CI credentials, providing a deterministic approach to the workflow across all affected components", C: "Pass credentials as command-line arguments to Claude Code in the CI script, ensuring consistent behavior across all sessions", D: "Use `~/.claude.json` in the CI runner with CI-specific credentials, using structured data for all inter-component communication" },
    correct: "A",
    explanations: { A: "CORRECT: Environment variable expansion (`${VARIABLE}`) in `.mcp.json` allows the same configuration file to work in both local development and CI. Each environment sets its own values for the environment variables (local dev from `.env`, CI from pipeline secrets).", B: "INCORRECT: Hardcoded CI credentials in `.mcp.json` would be committed to the repository, exposing sensitive credentials. This violates the no-hardcoded-credentials principle.", C: "INCORRECT: Claude Code MCP configuration is not typically passed via command-line arguments. The `.mcp.json` with environment variable expansion is the designed mechanism.", D: "INCORRECT: `~/.claude.json` is user-scoped. In CI, the 'user' is the CI runner, and managing `~/.claude.json` in CI adds configuration complexity. The project-level `.mcp.json` with env vars is cleaner." }
  },
  {
    id: "d2-063",
    domain: "D2",
    taskStatement: "2.4",
    topic: "MCP Integration",
    difficulty: "hard",
    scenario: 3,
    importance: "MCP server error handling and resilience",
    question: "A research system's MCP server for a financial data API crashes intermittently under high load. When the server crashes, Claude loses access to all tools from that server and proceeds without financial data, producing incomplete research reports. What is the recommended resilience pattern?",
    choices: { A: "Add a try-catch around every tool call to handle missing servers, following the recommended implementation pattern according to the configured policy", B: "Implement connection monitoring in the orchestrator that detects MCP server disconnection, attempts reconnection with exponential backoff, and pauses Claude's workflow until the server is restored or a timeout triggers a graceful degradation path", C: "Duplicate the MCP server tools as native tools so Claude has a fallback, with appropriate error handling at each step through the standard interface layer", D: "Add a health check that restarts the MCP server automatically when it crashes, with appropriate error handling at each step according to the configured policy" },
    correct: "B",
    explanations: { A: "INCORRECT: Per-call try-catch handles individual failures but doesn't address the systemic issue of server disconnection. It also doesn't provide recovery or degradation logic.", B: "CORRECT: Connection monitoring, reconnection with backoff, and workflow management during outages is the comprehensive resilience pattern. The orchestrator should detect disconnection, attempt recovery, and either pause the workflow (if the data is critical) or trigger a graceful degradation path (if partial results are acceptable).", C: "INCORRECT: Duplicating MCP tools as native tools doubles the maintenance burden and creates tool overlap confusion. MCP server reliability should be solved at the infrastructure level.", D: "INCORRECT: Auto-restart helps but there's a gap between crash and restart where Claude has no access. The orchestrator needs to be aware of the outage and handle the workflow appropriately during the gap." }
  },
  {
    id: "d2-064",
    domain: "D2",
    taskStatement: "2.4",
    topic: "MCP Integration",
    difficulty: "medium",
    scenario: 6,
    importance: "MCP tool naming and namespacing",
    question: "A structured data extraction system uses two MCP servers: one for PDF processing and one for image OCR. Both expose a tool called `extract_text`. When Claude calls `extract_text`, which server handles the request?",
    choices: { A: "It fails with a name collision error, following the recommended implementation pattern per the architectural requirements", B: "Both servers handle it in parallel, and Claude gets both results, supporting both automated and manual processes for production-grade reliability", C: "MCP tools are namespaced by server — they appear as `pdf_server__extract_text` and `ocr_server__extract_text` (or similar), so Claude must specify which one to call", D: "The first server in the configuration file handles it, scoped to the relevant context for this operation per the architectural requirements" },
    correct: "C",
    explanations: { A: "INCORRECT: MCP handles name collisions through namespacing rather than failing. The framework is designed to support multiple servers with potentially overlapping tool names.", B: "INCORRECT: MCP does not broadcast tool calls to multiple servers. Each call targets a specific server's tool.", C: "CORRECT: MCP tools from different servers are namespaced (typically `serverName__toolName` or similar prefixing) to prevent collisions. Claude sees them as distinct tools and must call the specific namespaced version. This means the tool descriptions should clearly differentiate when to use each server's `extract_text`.", D: "INCORRECT: MCP doesn't use first-match routing for tool names. Tool namespacing prevents ambiguity." }
  },
  {
    id: "d2-065",
    domain: "D2",
    taskStatement: "2.4",
    topic: "MCP Integration",
    difficulty: "hard",
    scenario: 4,
    importance: "MCP server capability discovery",
    question: "A developer productivity platform dynamically adds new tools to its MCP server (e.g., when new integrations are deployed). Claude Code was started this morning, and a new `deploy_canary` tool was added to the MCP server at noon. A developer tries to use it at 2 PM but Claude doesn't know about it. Why?",
    choices: { A: "MCP tools are discovered at connection time. Since Claude Code connected this morning, it has the morning's tool list. The developer needs to restart Claude Code or reconnect to the MCP server to discover new tools.", B: "The new tool wasn't added to `.mcp.json` so Claude Code can't see it, ensuring consistent behavior across all sessions to maintain system integrity", C: "The tool was added after Claude Code's context window was established and tools are cached for the session, using the standard configuration for this scenario to maintain system integrity", D: "MCP servers can only expose tools that existed when the server was first compiled, with clear separation between policy and implementation for production-grade reliability" },
    correct: "A",
    explanations: { A: "CORRECT: MCP tools are discovered when the client connects to the server. There is no automatic re-discovery or hot-reload of tools during an active session. If the server adds tools after connection, the client won't see them until reconnection.", B: "INCORRECT: `.mcp.json` configures server connections, not individual tools. The tools are exposed by the running MCP server, not listed in the config file.", C: "INCORRECT: This is partially right about caching, but the specific mechanism is MCP discovery at connection time, not context window establishment.", D: "INCORRECT: MCP servers can dynamically register tools at runtime. The issue is that the client discovers tools at connection time and doesn't re-discover them mid-session." }
  },
  {
    id: "d2-066",
    domain: "D2",
    taskStatement: "2.4",
    topic: "MCP Integration",
    difficulty: "hard",
    scenario: 1,
    importance: "MCP transport selection",
    question: "A customer support platform needs to connect Claude Code to an MCP server running in a Kubernetes cluster in a different network zone. The MCP server handles sensitive customer data. What transport configuration is appropriate?",
    choices: { A: "Run the MCP server locally and sync data from Kubernetes to avoid network transport with explicit dependency management", B: "Use stdio transport — it works for all MCP configurations, with explicit handling of edge cases and boundaries", C: "Use SSE (Server-Sent Events) transport over HTTPS with mutual TLS authentication for secure cross-network communication", D: "Use stdio transport with SSH tunneling to the Kubernetes pod, which addresses the core requirements effectively" },
    correct: "C",
    explanations: { A: "INCORRECT: Syncing sensitive customer data to a local machine creates data residency and security issues. The data should stay in its secured network zone.", B: "INCORRECT: stdio transport launches the server as a local subprocess. It doesn't support connecting to remote servers in different network zones.", C: "CORRECT: SSE (Server-Sent Events) transport is designed for remote MCP servers. Over HTTPS with mutual TLS, it provides encrypted, authenticated communication suitable for cross-network access to sensitive data. This is the appropriate transport for connecting to a remote MCP server in a different network zone.", D: "INCORRECT: stdio transport requires the server process to be local (started by the client). SSH tunneling to a Kubernetes pod is fragile and not the designed cross-network pattern." }
  },
  {
    id: "d2-067",
    domain: "D2",
    taskStatement: "2.4",
    topic: "MCP Integration",
    difficulty: "medium",
    scenario: 3,
    importance: "MCP prompts for guided workflows",
    question: "A research system's MCP server exposes complex analytical tools. New analysts struggle to use them effectively. The team wants to provide guided workflows like 'Quarterly Market Analysis' that set up the right tool sequence with pre-configured parameters. What MCP feature supports this?",
    choices: { A: "Add workflow instructions to the system prompt for each user session, which maintains reliability under production load using the established workflow conventions", B: "Use MCP Prompts — server-defined prompt templates that combine instructions, tool sequences, and pre-configured parameters into reusable guided workflows", C: "Create a separate 'workflow' tool that wraps other tools, integrated into the standard development pipeline with proper scope boundaries", D: "Add workflow descriptions to each tool's description field, with proper validation at each stage according to the configured policy" },
    correct: "B",
    explanations: { A: "INCORRECT: System prompt workflow instructions are session-specific and not reusable. MCP Prompts are defined on the server and available to all users, making them maintainable and consistent.", B: "CORRECT: MCP Prompts are server-defined prompt templates that can include pre-configured instructions, suggested tool sequences, and parameter defaults. They enable guided workflows without modifying tool definitions, making complex analytical processes accessible to new analysts.", C: "INCORRECT: A wrapper tool adds unnecessary indirection and makes debugging harder. MCP Prompts are the native mechanism for workflow guidance.", D: "INCORRECT: Tool descriptions guide individual tool selection, not multi-step workflows. Cramming workflow logic into a single tool's description overloads its purpose." }
  },
  {
    id: "d2-068",
    domain: "D2",
    taskStatement: "2.4",
    topic: "MCP Integration",
    difficulty: "hard",
    scenario: 4,
    importance: "MCP server testing and validation",
    question: "A team is building a custom MCP server for their internal deployment system. Before connecting it to Claude Code in production, they need to verify it works correctly. What is the recommended testing approach?",
    choices: { A: "Deploy to production and monitor for errors, with built-in recovery for expected failure modes for production-grade reliability", B: "Connect it to Claude Code directly and test manually with various prompts, using the standard configuration for this scenario with explicit dependency management", C: "Test only the backend API that the MCP server wraps — if the API works, the MCP server will work, aligned with the documented best practices with explicit dependency management", D: "Write unit tests for each tool handler, then use the MCP Inspector tool to test the server's MCP protocol compliance, tool discovery, parameter validation, and error handling before connecting to Claude Code" },
    correct: "D",
    explanations: { A: "INCORRECT: Deploying untested servers to production is reckless, especially for a deployment system that could cause outages if tools malfunction.", B: "INCORRECT: Manual testing with Claude Code is slow, non-reproducible, and can't systematically test edge cases. You can't control what Claude will call or in what order.", C: "INCORRECT: The backend API working doesn't guarantee the MCP layer is correct. Issues can arise in parameter mapping, response formatting, error translation, and protocol compliance — all specific to the MCP layer.", D: "CORRECT: A layered testing approach — unit tests for tool handlers, then MCP Inspector for protocol compliance — ensures the server works correctly at both the business logic and protocol levels. MCP Inspector can validate tool discovery, parameter schemas, error responses, and protocol compliance independently of Claude." }
  },
  {
    id: "d2-069",
    domain: "D2",
    taskStatement: "2.4",
    topic: "MCP Integration",
    difficulty: "medium",
    scenario: 5,
    importance: "MCP server performance in CI/CD",
    question: "A CI/CD pipeline using Claude Code with 4 MCP servers has a cold start time of 60 seconds due to server initialization. For a pipeline that runs hundreds of times daily, this adds significant time. What optimization reduces cold start impact?",
    choices: { A: "Run MCP servers as persistent background services that stay alive between pipeline runs, so subsequent runs connect to already-initialized servers", B: "Reduce the number of MCP servers to 1 by combining all functionality, which addresses the core requirements effectively while preserving existing behavior", C: "Use `tool_choice: none` to skip MCP initialization, with explicit handling of edge cases and boundaries with proper scope boundaries", D: "Remove MCP servers from CI and use direct API calls instead, which maintains reliability under production load across all affected components" },
    correct: "A",
    explanations: { A: "CORRECT: Running MCP servers as persistent background services (or long-lived processes) means initialization happens once and subsequent pipeline runs connect instantly. This amortizes the cold start cost across hundreds of daily runs.", B: "INCORRECT: Combining all functionality into one server reduces cold starts but creates a monolithic server that's harder to maintain and doesn't benefit from the modular MCP architecture.", C: "INCORRECT: `tool_choice: none` disables tool USE, not server initialization. The servers are still discovered and connected at startup regardless of tool_choice settings.", D: "INCORRECT: Replacing MCP with direct API calls loses the benefits of the MCP abstraction and requires maintaining custom API integration code." }
  },
  {
    id: "d2-070",
    domain: "D2",
    taskStatement: "2.4",
    topic: "MCP Integration",
    difficulty: "hard",
    scenario: 3,
    importance: "MCP sampling for LLM-mediated tool interactions",
    question: "A research MCP server needs to summarize large datasets before returning them to Claude (to fit within token limits). The server itself doesn't have summarization capability. What MCP feature allows the server to request LLM help for this preprocessing step?",
    choices: { A: "Add a summarization library to the MCP server, ensuring consistent behavior across all sessions while preserving existing behavior", B: "The MCP server should call the Claude API directly with its own API key, integrated into the standard development pipeline per the architectural requirements", C: "MCP Sampling allows the server to request an LLM completion through the client (Claude Code), enabling the server to leverage Claude's capabilities for preprocessing like summarization before returning the final result", D: "The server should truncate the data to fit the token limit without summarization, with built-in recovery for expected failure modes with proper scope boundaries" },
    correct: "C",
    explanations: { A: "INCORRECT: Adding a summarization library (likely a smaller model) to the MCP server adds infrastructure complexity and produces lower-quality summaries than Claude. MCP Sampling leverages the existing, high-quality LLM connection.", B: "INCORRECT: Having the MCP server make independent API calls to Claude creates a separate billing stream, loses conversation context, and requires managing API keys in the server.", C: "CORRECT: MCP Sampling is a protocol feature that allows MCP servers to request LLM completions through the connected client. The server can send the large dataset to the client with a summarization prompt, receive the summary, and then return the compact result. This keeps the LLM interaction within the existing session.", D: "INCORRECT: Truncation loses data and may remove critical information. Summarization preserves the key insights while reducing size." }
  },
  {
    id: "d2-071",
    domain: "D2",
    taskStatement: "2.4",
    topic: "MCP Integration",
    difficulty: "hard",
    scenario: 6,
    importance: "MCP for multi-format extraction pipelines",
    question: "A structured data extraction system processes PDFs, images, spreadsheets, and HTML documents. The team considers two MCP architectures: (A) one MCP server per document format, each with format-specific extraction tools, or (B) one MCP server with a single `extract(document_url, format_hint)` tool. The system handles 10,000 documents daily across all formats. Which architecture is better and why?",
    choices: { A: "Architecture A because MCP requires one server per document type, with appropriate error handling at each step across all affected components", B: "Architecture B (single server) because it's simpler and fewer MCP servers mean faster startup while preserving existing behavior", C: "Architecture A (per-format servers) because each server can be optimized for its format and scaled independently based on document volume per format", D: "Architecture B because Claude can't handle more than one MCP server, maintaining compatibility with existing workflows using the established workflow conventions" },
    correct: "C",
    explanations: { A: "INCORRECT: MCP does not require one server per document type. This is an architectural choice, not a protocol requirement.", B: "INCORRECT: A single server is simpler initially but becomes a bottleneck at scale. It can't be optimized or scaled per format, and a bug in one format's handler can affect all formats.", C: "CORRECT: Per-format MCP servers can be independently optimized (PDF processing uses different libraries than image OCR), independently scaled (if 70% of documents are PDFs, scale the PDF server), and independently maintained. At 10,000 documents daily, operational flexibility matters significantly.", D: "INCORRECT: Claude Code can connect to multiple MCP servers simultaneously. There is no single-server limitation." }
  },
  {
    id: "d2-072",
    domain: "D2",
    taskStatement: "2.4",
    topic: "MCP Integration",
    difficulty: "medium",
    scenario: 4,
    importance: "MCP configuration best practices",
    question: "A developer's `.mcp.json` looks like: `{ \"mcpServers\": { \"github\": { \"command\": \"npx\", \"args\": [\"-y\", \"@modelcontextprotocol/server-github\"], \"env\": { \"GITHUB_TOKEN\": \"${GITHUB_TOKEN}\" } } } }`. The developer's `GITHUB_TOKEN` environment variable is not set. What happens when Claude Code starts?",
    choices: { A: "Claude Code fails to start entirely because of the unresolved environment variable, scoped to the relevant context for this operation", B: "The `${GITHUB_TOKEN}` string is passed literally as the token value, scoped to the relevant context for this operation for production-grade reliability", C: "The GitHub MCP server fails to connect or authenticate, but Claude Code starts and other tools/servers work. Claude won't have access to GitHub tools.", D: "Claude Code starts normally and the GitHub MCP server works without authentication, with clear separation between policy and implementation" },
    correct: "C",
    explanations: { A: "INCORRECT: A single MCP server failure typically doesn't prevent Claude Code from starting. Other servers and built-in tools remain available.", B: "INCORRECT: MCP configuration supports `${VARIABLE}` expansion. An unset variable typically resolves to an empty string or causes a connection error, not literal passthrough of the template syntax.", C: "CORRECT: When the environment variable is unresolved, the GitHub MCP server will fail to authenticate with the GitHub API. This server will be unavailable, and Claude won't have access to GitHub tools. However, Claude Code itself continues to function with other available tools and servers.", D: "INCORRECT: The GitHub MCP server requires authentication. Without a token, it cannot authenticate with the GitHub API." }
  },

  // ============================================================
  // Task 2.5: Built-in Tools (18 questions)
  // ============================================================
  {
    id: "d2-073",
    domain: "D2",
    taskStatement: "2.5",
    topic: "Built-in Tools",
    difficulty: "hard",
    scenario: 4,
    importance: "Grep vs Glob — commonly confused",
    question: "A developer asks Claude Code: 'Find all TypeScript files in the project that import the `AuthProvider` component.' Claude uses Glob with pattern `**/*.ts` and then reads each file to check for the import. This takes 3 minutes across 500 files. What is the correct approach?",
    choices: { A: "Use Glob with `**/AuthProvider*` to find the component file, then trace all imports from there for production-grade reliability", B: "Use Glob with a more specific pattern like `**/*auth*.ts` to narrow the file list, maintaining compatibility with existing workflows", C: "Use Read on the `tsconfig.json` to find the import paths, then Glob for those specific paths, following established patterns for this architecture", D: "Use Grep to search for the pattern `import.*AuthProvider` across the codebase, which directly finds files containing the import without reading every file" },
    correct: "D",
    explanations: { A: "INCORRECT: Finding the component file doesn't reveal its consumers. Import relationships go from consumer to provider, so you need to search consumer files for the import statement.", B: "INCORRECT: Glob matches file paths, not file contents. Even with `*auth*` in the path, many files that import `AuthProvider` won't have 'auth' in their filename. This filters the wrong dimension.", C: "INCORRECT: `tsconfig.json` contains compiler configuration, not import relationships. This approach doesn't help find which files import a specific component.", D: "CORRECT: Grep is for content search — it finds files containing specific text patterns. Searching for `import.*AuthProvider` directly identifies every file that imports the component, regardless of filename. This is the correct tool for content-based queries and is dramatically faster than reading every file." }
  },
  {
    id: "d2-074",
    domain: "D2",
    taskStatement: "2.5",
    topic: "Built-in Tools",
    difficulty: "medium",
    scenario: 4,
    importance: "Glob for path-based file discovery",
    question: "A developer asks Claude Code: 'What test files exist for the payments module?' The codebase follows the convention of `*.test.ts` files next to source files. What is the most efficient approach?",
    choices: { A: "Read the directory listing for every folder in the payments module for production-grade reliability", B: "Use Glob with pattern `**/payments/**/*.test.ts` to find all test files in the payments module by path pattern", C: "Use Grep for 'payments' to find files mentioning the module, aligned with the documented best practices", D: "Grep for 'describe(' or 'it(' to find all test files, following the recommended implementation pattern" },
    correct: "B",
    explanations: { A: "INCORRECT: Manually reading directory listings is slow and tedious. Glob does exactly this job — finding files by path pattern — in a single operation.", B: "CORRECT: Glob is designed for path-based file discovery. The pattern `**/payments/**/*.test.ts` efficiently finds all files matching the test naming convention within the payments directory tree. This is the exact use case for Glob.", C: "INCORRECT: Grepping for 'payments' would find any file that mentions payments in its content, which includes documentation, configuration, and unrelated code that references the payments module.", D: "INCORRECT: Grepping for test function keywords would find test files across the ENTIRE codebase, not just the payments module. You'd need additional filtering by path, which Glob does natively." }
  },
  {
    id: "d2-075",
    domain: "D2",
    taskStatement: "2.5",
    topic: "Built-in Tools",
    difficulty: "hard",
    scenario: 4,
    importance: "Edit fails on non-unique text — Read+Write fallback",
    question: "A developer asks Claude Code to change a specific `console.log('debug')` statement on line 145 of a file. The file has 12 identical `console.log('debug')` statements. Claude uses Edit to replace the text but the operation fails. Why, and what is the correct fallback?",
    choices: { A: "Edit failed because the file is too large. Use Write to rewrite the entire file., with clear separation between policy and implementation to maintain system integrity", B: "Edit failed because line numbers aren't supported. Use Grep to find the exact occurrence first., providing a deterministic approach to the workflow across all affected components", C: "Edit failed because of a syntax error. Fix the syntax and retry Edit., with clear separation between policy and implementation using the established workflow conventions", D: "Edit failed because `console.log('debug')` is not unique in the file — Edit requires the target text to appear exactly once. The fallback is to Read the file to get the surrounding context, then use Edit with a larger unique text snippet that includes lines around line 145." },
    correct: "D",
    explanations: { A: "INCORRECT: Edit doesn't fail due to file size. The failure is specifically because the target text appears multiple times, making the replacement ambiguous.", B: "INCORRECT: Grep finds content in files but doesn't help Edit identify which occurrence to replace. The uniqueness constraint is on Edit's old_string parameter.", C: "INCORRECT: There's no syntax error — `console.log('debug')` is valid. The issue is uniqueness, not syntax.", D: "CORRECT: Edit requires the old_string to be unique in the file. When it's not (12 identical occurrences), Edit fails. The correct fallback is to Read the file to see the context around line 145, then use Edit with a larger text snippet that includes surrounding lines, making the target unique. If that's still not unique, Read+Write (rewriting the full file) is the final fallback." }
  },
  {
    id: "d2-076",
    domain: "D2",
    taskStatement: "2.5",
    topic: "Built-in Tools",
    difficulty: "medium",
    scenario: 4,
    importance: "Incremental exploration pattern",
    question: "A developer asks Claude Code to understand how errors propagate through a large Express.js application. The codebase has 200 files. What is the correct incremental exploration strategy?",
    choices: { A: "Use Glob to find all `.ts` files and read them in alphabetical order, supporting both automated and manual processes while preserving existing behavior", B: "Read `package.json` to understand dependencies, then read the README for architecture overview, following established patterns for this architecture to maintain system integrity", C: "Read every file in the `src/` directory to build a complete mental model, with clear separation between policy and implementation to maintain system integrity", D: "Start with Grep to find error handling entry points (e.g., `app.use.*error`, `catch`, `next(err)`), then Read the relevant files, trace imports, and follow the error flow incrementally" },
    correct: "D",
    explanations: { A: "INCORRECT: Alphabetical file reading is arbitrary and has no relation to error flow. Most files won't be relevant to error handling.", B: "INCORRECT: `package.json` and README provide high-level context but don't reveal the actual error propagation implementation. Starting with code search is more direct.", C: "INCORRECT: Reading all 200 files wastes time and context window. Most files are irrelevant to error handling. The incremental approach focuses on relevant code first.", D: "CORRECT: The incremental exploration pattern starts with Grep to find entry points (error middleware, catch blocks, error forwarding), then uses Read to examine the relevant files, traces imports to understand dependencies, and follows the error flow from handler to handler. This is efficient and focused." }
  },
  {
    id: "d2-077",
    domain: "D2",
    taskStatement: "2.5",
    topic: "Built-in Tools",
    difficulty: "hard",
    scenario: 2,
    importance: "Start with Grep for entry points, not reading all files",
    question: "A developer using Claude Code for code generation needs to add a new REST endpoint that follows the existing patterns. The codebase has 300+ files. Claude should understand the routing pattern, middleware chain, and response format before generating code. What is the correct exploration sequence?",
    choices: { A: "Use Glob to find all route files, Read all of them, then synthesize the pattern, following the recommended implementation pattern while preserving existing behavior", B: "Read the main entry file (app.ts or index.ts), then read every imported file recursively until the full architecture is understood with explicit dependency management", C: "Grep for `router.get` or `app.get` to find existing endpoint definitions, Read 2-3 representative examples to understand the pattern, then generate the new endpoint matching the established conventions", D: "Ask the developer to describe the architecture, then generate code based on the description, supporting both automated and manual processes while preserving existing behavior" },
    correct: "C",
    explanations: { A: "INCORRECT: Globbing for route files and reading ALL of them is overkill. 2-3 representative examples are sufficient to understand the pattern.", B: "INCORRECT: Recursively reading all imports from the entry point would eventually read most of the 300+ files. This is the 'read everything' anti-pattern that wastes context window and time.", C: "CORRECT: Starting with Grep to find existing endpoints gives Claude a focused set of entry points. Reading 2-3 representative examples reveals the routing pattern, middleware usage, and response format. This is enough to generate a consistent new endpoint without reading the entire codebase.", D: "INCORRECT: Developer descriptions may be incomplete or outdated. Examining actual code ensures the generated endpoint matches the real implementation patterns." }
  },
  {
    id: "d2-078",
    domain: "D2",
    taskStatement: "2.5",
    topic: "Built-in Tools",
    difficulty: "hard",
    scenario: 4,
    importance: "Combining built-in tools for complex investigations",
    question: "A developer reports a bug: 'The `calculateDiscount` function returns wrong values for premium customers.' Claude Code needs to investigate. The function exists somewhere in 150 files. What is the optimal tool sequence?",
    choices: { A: "Use Grep for `premium` across the entire codebase to find all premium-related logic, using structured data for all inter-component communication while preserving existing behavior", B: "Read the test files first to understand expected behavior, then find the implementation, aligned with the documented best practices according to the configured policy", C: "Grep for `calculateDiscount` → Read the file containing the definition → Grep for `premium` in that file → Read surrounding logic → Grep for callers of `calculateDiscount` to trace the data flow", D: "Glob for `*discount*` files → Read all of them → Identify the bug by reading, maintaining compatibility with existing workflows with explicit dependency management" },
    correct: "C",
    explanations: { A: "INCORRECT: Grepping for 'premium' across 150 files would return too many irrelevant results (UI text, documentation, other premium-related code). Starting with the specific function name is more targeted.", B: "INCORRECT: Tests might not exist for this function, or they might not cover the premium case. Starting with the implementation is more direct for bug investigation.", C: "CORRECT: This follows the optimal incremental investigation pattern: (1) Grep finds the function definition quickly, (2) Read examines the implementation, (3) Grep within the file focuses on the premium customer logic, (4) Read the surrounding code to understand the bug, (5) Grep for callers traces how data flows into the function. Each step narrows the investigation.", D: "INCORRECT: Globbing for `*discount*` finds files by name, but the function might be in a file not named 'discount'. Also, reading all matched files is wasteful." }
  },
  {
    id: "d2-079",
    domain: "D2",
    taskStatement: "2.5",
    topic: "Built-in Tools",
    difficulty: "medium",
    scenario: 5,
    importance: "Built-in tools in CI/CD context",
    question: "In a CI/CD pipeline, Claude Code needs to verify that a pull request doesn't introduce any `console.log` statements in production code (but they're allowed in test files). What built-in tool approach accomplishes this?",
    choices: { A: "Use Grep with pattern `console\\.log` excluding test file patterns (e.g., `--glob '!**/*.test.*'`) to find `console.log` only in production code", B: "Read every changed file and visually scan for `console.log`, which maintains reliability under production load per the architectural requirements", C: "Use Glob to find all `.js` files and then Read each one, with proper validation at each stage while preserving existing behavior", D: "Use Edit to remove all `console.log` statements automatically, providing a deterministic approach to the workflow using the established workflow conventions" },
    correct: "A",
    explanations: { A: "CORRECT: Grep with exclusion patterns efficiently finds `console.log` statements while ignoring test files. This is precisely the content-search-with-filtering use case that Grep excels at.", B: "INCORRECT: Manually reading every changed file is slow and error-prone, especially for large PRs. Grep is purpose-built for content search.", C: "INCORRECT: Glob finds files by path pattern, not by content. You'd still need to read every file to check for `console.log`, which is what Grep does in one step.", D: "INCORRECT: Automatically removing `console.log` is destructive and might remove intentional logging. The task is to verify (detect), not to fix." }
  },
  {
    id: "d2-080",
    domain: "D2",
    taskStatement: "2.5",
    topic: "Built-in Tools",
    difficulty: "hard",
    scenario: 4,
    importance: "Read tool for targeted file examination",
    question: "Claude Code needs to examine a 5,000-line configuration file to find the database connection settings. A developer says the settings are 'somewhere in the middle.' What is the most efficient approach?",
    choices: { A: "Grep for `database` or `connection` in the file to find the exact line numbers, then Read only the relevant section using offset and limit parameters", B: "Read the file in 500-line chunks from the beginning until finding the settings, ensuring consistent behavior across all sessions", C: "Read the entire 5,000-line file to find the database settings, following established patterns for this architecture across all affected components", D: "Use Edit to search-and-replace the database settings without reading first, using structured data for all inter-component communication" },
    correct: "A",
    explanations: { A: "CORRECT: Grep first identifies the exact line numbers where database/connection settings appear. Then Read with offset and limit parameters reads only the relevant section (e.g., lines 2400-2450). This uses minimal context window while providing exactly the needed information.", B: "INCORRECT: Sequential chunk reading is a linear search — you might need to read 2500+ lines before finding the settings. Grep provides instant location.", C: "INCORRECT: Reading a 5,000-line file consumes significant context window space, mostly with irrelevant configuration. Targeted reading is much more efficient.", D: "INCORRECT: Edit requires knowing the exact text to replace. You can't use Edit to search without first knowing what's in the file. Also, you should always Read before Edit." }
  },
  {
    id: "d2-081",
    domain: "D2",
    taskStatement: "2.5",
    topic: "Built-in Tools",
    difficulty: "medium",
    scenario: 2,
    importance: "Write tool for new file creation",
    question: "A developer asks Claude Code to create a new utility module `src/utils/date-formatter.ts`. The file doesn't exist yet. What is the correct tool to use?",
    choices: { A: "Edit to create the file by specifying the new content, integrated into the standard development pipeline", B: "Write to create the new file with the complete content, since Edit is for modifying existing files", C: "Read the file first (it will fail), then use Edit to create it per the architectural requirements", D: "Use Bash to run `touch src/utils/date-formatter.ts` then use Edit to add content for production-grade reliability" },
    correct: "B",
    explanations: { A: "INCORRECT: Edit modifies existing files by replacing specific text. It's not designed for creating new files from scratch — it requires an old_string to replace.", B: "CORRECT: Write is the tool for creating new files. It takes a file path and complete content. For new file creation where no existing file needs to be modified, Write is the correct choice.", C: "INCORRECT: Intentionally failing a Read and then using Edit is an unnecessary extra step. Write directly creates new files.", D: "INCORRECT: Using Bash to create an empty file and then Edit to add content is two steps when Write accomplishes both in one operation." }
  },
  {
    id: "d2-082",
    domain: "D2",
    taskStatement: "2.5",
    topic: "Built-in Tools",
    difficulty: "hard",
    scenario: 4,
    importance: "Tool selection for refactoring tasks",
    question: "A developer asks Claude Code to rename the function `processData` to `transformPayload` across the entire codebase (50 files reference it). What is the optimal tool strategy?",
    choices: { A: "Use Edit with `replace_all: true` on each file found by Grep — first Grep to identify all files containing `processData`, then Edit each file with `old_string: 'processData'` and `new_string: 'transformPayload'` with `replace_all` to catch multiple occurrences per file", B: "Read every file in `src/`, find mentions of `processData`, and use Write to rewrite each file, providing a deterministic approach to the workflow per the architectural requirements", C: "Use Bash to run `sed -i 's/processData/transformPayload/g'` across all files, using structured data for all inter-component communication per the architectural requirements", D: "Grep for `processData` to find all files that reference it, then use Edit on each file to replace `processData` with `transformPayload`, scoped to the relevant context for this operation while preserving existing behavior" },
    correct: "A",
    explanations: { A: "CORRECT: Grep identifies all 50 files containing `processData`. Then Edit with `replace_all: true` on each file replaces every occurrence within that file. This is precise, safe (only touching files that need changes), and handles multiple occurrences per file.", B: "INCORRECT: Reading every file is wasteful. Grep efficiently identifies only the files that contain the reference. Also, Write rewrites the entire file, which is riskier than targeted Edit replacements.", C: "INCORRECT: While `sed` would work technically, using built-in tools (Grep + Edit) provides better error handling, confirmation of changes, and integration with Claude Code's workflow. Built-in tools are preferred when they can accomplish the task.", D: "INCORRECT: Using Edit without `replace_all` would fail on files that have multiple references to `processData`, since Edit requires unique text matches by default." }
  },
  {
    id: "d2-083",
    domain: "D2",
    taskStatement: "2.5",
    topic: "Built-in Tools",
    difficulty: "hard",
    scenario: 3,
    importance: "Built-in tools for research codebase exploration",
    question: "A research team asks Claude Code to analyze a data processing pipeline codebase they've never seen before. The codebase has 400 files with no documentation. What is the recommended exploration strategy using built-in tools?",
    choices: { A: "Use Grep for 'pipeline' to find all pipeline-related code and read everything, maintaining compatibility with existing workflows using the established workflow conventions", B: "Glob for `**/*.py` to list all Python files, then read them in order of modification date, maintaining compatibility with existing workflows using the established workflow conventions", C: "Start with Glob to understand the directory structure (`**/*/`), then Grep for entry points (e.g., `if __name__`, `def main`, `app.run`), Read the entry files, and incrementally trace the pipeline through imports and function calls", D: "Read the README.md for an overview, then read the main entry point, using structured data for all inter-component communication using the established workflow conventions" },
    correct: "C",
    explanations: { A: "INCORRECT: Grepping for 'pipeline' might miss code that doesn't use that exact word and might match comments or variable names. Starting with structural entry points is more reliable.", B: "INCORRECT: Reading all 400 Python files in modification order is a brute-force approach that wastes context. Modification date order doesn't reflect code flow.", C: "CORRECT: This is the recommended incremental exploration pattern: (1) Glob for directory structure reveals the project organization, (2) Grep for entry points identifies where execution begins, (3) Read the entry files to understand the top-level flow, (4) incrementally trace through imports and function calls to understand the pipeline. Each step narrows the exploration.", D: "INCORRECT: The question states there's no documentation, so README.md likely doesn't exist or is unhelpful. The entry point filename is unknown." }
  },
  {
    id: "d2-084",
    domain: "D2",
    taskStatement: "2.5",
    topic: "Built-in Tools",
    difficulty: "medium",
    scenario: 6,
    importance: "Using built-in tools for data format investigation",
    question: "A structured data extraction system needs to understand the format of incoming CSV files before writing extraction logic. The CSV files are large (50,000 rows). What is the efficient approach with built-in tools?",
    choices: { A: "Read the entire CSV file to understand the format, integrated into the standard development pipeline per the architectural requirements", B: "Use Bash to run `wc -l` to count rows first, with proper validation at each stage per the architectural requirements", C: "Use Read with `limit: 20` to examine just the header row and first few data rows to understand the column structure and data formats", D: "Use Grep to search for specific column names across the file, applied systematically across the entire system with explicit dependency management" },
    correct: "C",
    explanations: { A: "INCORRECT: Reading a 50,000-row CSV consumes enormous context window space. The format can be determined from a small sample.", B: "INCORRECT: Row count alone doesn't reveal the format. You need to see the actual header and data structure.", C: "CORRECT: Read with `limit: 20` examines the header row and a few data rows, which is sufficient to understand column names, data types, delimiters, and formatting conventions. This uses minimal context while providing all the information needed to write extraction logic.", D: "INCORRECT: Grepping for column names assumes you already know what columns exist. The task is to discover the format, not search for known patterns." }
  },
  {
    id: "d2-085",
    domain: "D2",
    taskStatement: "2.5",
    topic: "Built-in Tools",
    difficulty: "hard",
    scenario: 4,
    importance: "Bash tool for operations outside Read/Write/Edit scope",
    question: "A developer asks Claude Code to check if the project's test suite passes before making changes. The test command is `npm test`. Which built-in tool is appropriate?",
    choices: { A: "Grep for `test` in `package.json` to find the test command, then tell the developer to run it manually", B: "Read the test files to verify they look correct, then confirm tests should pass", C: "Use Write to create a test runner script, then Bash to execute it", D: "Use the Bash tool to execute `npm test` and evaluate the results" },
    correct: "D",
    explanations: { A: "INCORRECT: Finding the test command is useful, but telling the developer to run it manually defeats the purpose of Claude Code. The Bash tool can execute commands directly.", B: "INCORRECT: Reading test files doesn't execute them. Tests need to actually run to verify they pass — static analysis of test code is not equivalent to running tests.", C: "INCORRECT: Creating a wrapper script for a command that can be run directly is unnecessary. `npm test` is a standard command that Bash can execute directly.", D: "CORRECT: The Bash tool executes shell commands, which is the correct tool for running tests, build commands, and other operations that go beyond file reading/writing/editing. Claude Code can then evaluate the output to determine if tests pass." }
  },
  {
    id: "d2-086",
    domain: "D2",
    taskStatement: "2.5",
    topic: "Built-in Tools",
    difficulty: "hard",
    scenario: 2,
    importance: "Edit tool requires reading the file first",
    question: "A developer asks Claude Code to fix a bug in `src/services/payment.ts` by changing `amount * 0.1` to `amount * taxRate`. Claude immediately uses Edit without reading the file first. What happens?",
    choices: { A: "Edit succeeds because it can find and replace text without reading context, which maintains reliability under production load across all affected components", B: "Edit automatically reads the file as part of its operation, using the standard configuration for this scenario with proper scope boundaries", C: "Edit succeeds but might replace the wrong occurrence because Claude didn't verify context, with explicit handling of edge cases and boundaries", D: "Edit fails because the tool requires that Read has been called on the file first in the conversation — Claude must Read the file to understand context before editing" },
    correct: "D",
    explanations: { A: "INCORRECT: Claude Code's Edit tool enforces that the file must have been Read first. This is a safety mechanism, not just a best practice.", B: "INCORRECT: Edit does not automatically read the file. The Read must be performed as a separate, prior step. This separation ensures Claude actively examines the file context before making changes.", C: "INCORRECT: The Edit tool will not proceed without a prior Read. The safety mechanism prevents this scenario.", D: "CORRECT: The Edit tool requires that Read has been called on the file first in the conversation. This ensures Claude has context about the file's structure, surrounding code, and the specific location of the change. Attempting to Edit without prior Read will fail." }
  },
  {
    id: "d2-087",
    domain: "D2",
    taskStatement: "2.5",
    topic: "Built-in Tools",
    difficulty: "medium",
    scenario: 4,
    importance: "Grep regex patterns for precise searches",
    question: "A developer asks Claude Code to find all TODO comments that have a priority marker like `TODO(P1):` or `TODO(P2):` but NOT generic `TODO:` comments. What Grep pattern accomplishes this?",
    choices: { A: "`P1|P2` to find priority markers", B: "`TODO`, following established patterns for this architecture", C: "`TODO\\(P[0-9]\\):`", D: "`TODO:` with manual filtering after results return" },
    correct: "C",
    explanations: { A: "INCORRECT: `P1|P2` would match any occurrence of P1 or P2 in the codebase, including variable names, version numbers, and other unrelated text.", B: "INCORRECT: Plain `TODO` matches all TODO comments including generic ones without priority markers. This is too broad.", C: "CORRECT: The regex `TODO\\(P[0-9]\\):` matches exactly the pattern: the literal text 'TODO', followed by a parenthesized priority marker (P0-P9), followed by a colon. The parentheses must be escaped in regex. This precisely matches `TODO(P1):`, `TODO(P2):`, etc.", D: "INCORRECT: Searching for `TODO:` and then manually filtering is inefficient. Grep's regex capability should be used to narrow results at search time." }
  },
  {
    id: "d2-088",
    domain: "D2",
    taskStatement: "2.5",
    topic: "Built-in Tools",
    difficulty: "hard",
    scenario: 5,
    importance: "Built-in tools for CI/CD code verification",
    question: "In a CI/CD pipeline, Claude Code needs to verify that a migration file follows the team's convention: every migration must have both an `up()` and a `down()` function. Given a new migration file at `migrations/20250115_add_user_roles.ts`, what is the verification approach?",
    choices: { A: "Use Glob to verify the file exists, then assume it follows the convention, with proper validation at each stage while preserving existing behavior", B: "Read the file to verify the convention, but also Grep across all migration files (`migrations/*.ts`) for any that might be missing `down()` — catching both the new file and any previously missed violations", C: "Grep for `function up` and `function down` in the migration file, verifying both exist, with explicit handling of edge cases and boundaries across all affected components", D: "Read the file and check for both `up()` and `down()` function definitions, ensuring consistent behavior across all sessions through the standard interface layer" },
    correct: "B",
    explanations: { A: "INCORRECT: File existence doesn't verify content. Convention compliance requires examining the file's contents.", B: "CORRECT: The comprehensive approach reads the new file for full context AND uses Grep across all migration files to catch any violations. This is especially valuable in CI/CD where you want to enforce conventions across the entire codebase, not just the latest change. It catches both new violations and historical ones.", C: "INCORRECT: Grep for a single file works but misses the broader verification opportunity. Also, the function might be defined as `async up()` or `export function up()` or `up = async () =>`, requiring a more flexible search.", D: "INCORRECT: Reading only the new file catches the immediate issue but misses an opportunity. In CI/CD, it's valuable to verify all migrations, not just the latest." }
  },
  {
    id: "d2-089",
    domain: "D2",
    taskStatement: "2.5",
    topic: "Built-in Tools",
    difficulty: "hard",
    scenario: 4,
    importance: "Multi-step debugging with built-in tools",
    question: "A developer reports that their Express middleware chain has a subtle bug where authentication is bypassed for certain routes. Claude Code needs to trace the middleware registration order. The app uses multiple router files that are dynamically imported. What is the correct investigation approach?",
    choices: { A: "Read `app.ts` to see all middleware registration, which gives the complete picture, scoped to the relevant context for this operation for production-grade reliability", B: "Grep for `app.use` in `app.ts` → Read the main app file → Grep for `router.use` across all files → Read each router file to trace the middleware chain → Grep for the authentication middleware specifically to verify its position relative to the affected routes", C: "Run the application with debug logging via Bash to observe the middleware execution order, supporting both automated and manual processes using the established workflow conventions", D: "Grep for `authenticate` or `auth` to find the middleware definition, then read only that file, applied systematically across the entire system with explicit dependency management" },
    correct: "B",
    explanations: { A: "INCORRECT: Express apps typically split middleware and routes across multiple files via `require`/`import`. The main app file shows top-level middleware but not route-specific middleware in router files.", B: "CORRECT: This multi-step investigation uses Grep to find middleware registration patterns, Read to examine the chain in context, and then targeted Grep to verify the authentication middleware's position. This systematically traces the middleware chain from top-level to route-specific, revealing where authentication might be missing or out-of-order.", C: "INCORRECT: Running the application might work but doesn't give Claude direct insight into the code structure. Static analysis with built-in tools is more precise for tracing registration order.", D: "INCORRECT: Finding only the auth middleware definition tells you what it does, not where it's registered in the chain. The bug is about registration order, not the middleware's implementation." }
  },
  {
    id: "d2-090",
    domain: "D2",
    taskStatement: "2.5",
    topic: "Built-in Tools",
    difficulty: "medium",
    scenario: 6,
    importance: "Choosing between Read and Grep for known vs unknown locations",
    question: "In two scenarios: (A) A developer says 'Check the database config in `config/database.yml`' and (B) A developer says 'Find where we configure the database connection.' What is the correct tool for each scenario?",
    choices: { A: "Scenario A: Read (known file path, examine directly). Scenario B: Grep (unknown location, search for content like 'database' or 'connection' across the codebase)", B: "Scenario A: Glob for `**/database.*`. Scenario B: Read `config/` directory listing, integrated into the standard development pipeline while preserving existing behavior", C: "Grep for both — always search for content patterns, following the recommended implementation pattern with explicit dependency management", D: "Read for both — always read files to find information, with explicit handling of edge cases and boundaries across all affected components" },
    correct: "A",
    explanations: { A: "CORRECT: When the file location is known, Read is the most direct tool. When the location is unknown but you know what content you're looking for, Grep searches across the codebase efficiently. This is the fundamental Read vs. Grep decision pattern: known location → Read, unknown location → Grep.", B: "INCORRECT: Glob finds files by path pattern, not content. In Scenario B, the config might not have 'database' in its filename. Read on a directory doesn't work — you need Read on a file.", C: "INCORRECT: Using Grep when you already know the exact file adds unnecessary overhead. Read directly accesses the known file.", D: "INCORRECT: Using Read when you don't know the file location means reading files randomly. Grep is designed for discovering content locations." }
  }
];
